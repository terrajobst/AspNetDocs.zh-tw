---
uid: web-forms/overview/moving-to-aspnet-20/data-bound-controls
title: 資料繫結控制項 |Microsoft Docs
author: microsoft
description: 大部分的 ASP.NET 應用程式都依賴來自後端資料來源的某種程度的資料呈現。 資料繫結控制項已是互動 w 的 pivotal 部分 。
ms.author: riande
ms.date: 02/20/2005
ms.assetid: 0e23ff32-646d-43f3-8bec-6b2313d3abd6
msc.legacyurl: /web-forms/overview/moving-to-aspnet-20/data-bound-controls
msc.type: authoredcontent
ms.openlocfilehash: 1154b38e0fa3d9d56cb407ae659c3b0d69871fda
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 03/06/2020
ms.locfileid: "78603835"
---
# <a name="data-bound-controls"></a>資料繫結控制項

由[Microsoft](https://github.com/microsoft)

> 大部分的 ASP.NET 應用程式都依賴來自後端資料來源的某種程度的資料呈現。 資料繫結控制項是在動態 Web 應用程式中與資料互動的 pivotal 部分。 ASP.NET 2.0 引進了資料繫結控制項的一些重大改進，包括新的 BaseDataBoundControl 類別和宣告式語法。

大部分的 ASP.NET 應用程式都依賴來自後端資料來源的某種程度的資料呈現。 資料繫結控制項是在動態 Web 應用程式中與資料互動的 pivotal 部分。 ASP.NET 2.0 引進了資料繫結控制項的一些重大改進，包括新的 BaseDataBoundControl 類別和宣告式語法。

BaseDataBoundControl 會作為 DataBoundControl 類別和 HierarchicalDataBoundControl 類別的基類。 在此課程模組中，我們將討論下列衍生自 DataBoundControl 的類別：

- AdRotator
- 清單控制項
- GridView
- FormView
- DetailsView

我們也會討論下列衍生自 HierarchicalDataBoundControl 類別的類別：

- TreeView
- 功能表
- SiteMapPath

## <a name="databoundcontrol-class"></a>DataBoundControl 類別

DataBoundControl 類別是抽象類別（在 VB 中標示為 MustInherit），用來與表格式或清單樣式資料互動。 下列控制項是衍生自 DataBoundControl 的一些控制項。

## <a name="adrotator"></a>AdRotator

AdRotator 控制項可讓您在連結至特定 URL 的網頁上顯示圖形橫幅。 顯示的圖形會使用控制項的屬性來旋轉。 您可以使用 [**印象**] 屬性來設定頁面上顯示的特定廣告頻率，而且可以使用關鍵字篩選來篩選廣告。

AdRotator 控制項使用 XML 檔案或資料庫中的資料表來取得資料。 下列屬性會在 XML 檔案中用來設定 AdRotator 控制項。

### <a name="imageurl"></a>ImageUrl
要針對廣告顯示之影像的 URL。

### <a name="navigateurl"></a>NavigateUrl
使用者在按一下廣告時應採取的 URL。 這應該是 URL 編碼。

### <a name="alternatetext"></a>AlternateText
顯示在工具提示中並由螢幕讀取器讀取的替代文字。 當 ImageUrl 所指定的影像無法使用時，也會顯示。

### <a name="keyword"></a>關鍵字
定義使用關鍵字篩選時可以使用的關鍵字。 如果指定，則只會顯示關鍵字符合關鍵字篩選準則的 ads。

### <a name="impressions"></a>形成
加權數位，決定特定廣告可能出現的頻率。 相對於相同檔案中其他廣告的印象。 XML 檔案中所有廣告的集體印象最大值為 2048000000 1。

### <a name="height"></a>高度
廣告的高度（以圖元為單位）。

### <a name="width"></a>寬度
廣告的寬度（以圖元為單位）。

> [!NOTE]
> Height 和 Width 屬性會覆寫 AdRotator 控制項本身的高度和寬度。

一般的 XML 檔案可能如下所示：

[!code-xml[Main](data-bound-controls/samples/sample1.xml)]

在上述範例中，Contoso 的 ad 可能會因為 [印象] 屬性的值而顯示為 ASP.NET 網站的廣告兩倍。

若要顯示上述 XML 檔案中的廣告，請將 AdRotator 控制項新增至頁面，並將**AdvertisementFile**屬性設定為指向 XML 檔案，如下所示：

[!code-aspx[Main](data-bound-controls/samples/sample2.aspx)]

如果您選擇使用資料庫資料表當做您的 AdRotator 控制項的資料來源，您必須先使用下列架構來設定資料庫：

| **資料行名稱** | **Data type** | **說明** |
| --- | --- | --- |
| 識別碼 | int | 主索引鍵。 此資料行可以有任何名稱。 |
| ImageUrl | Nvarchar （*長度*） | 要為廣告顯示之影像的相對或絕對 URL。 |
| NavigateUrl | Nvarchar （*長度*） | Ad 的目標 URL。 如果您未提供值，則廣告不是超連結。 |
| AlternateText | Nvarchar （*長度*） | 找不到影像時所顯示的文字。 在某些瀏覽器中，文字會顯示為工具提示。 替代文字也會用於協助工具，如此一來，看不到圖形的使用者可能會聽到朗讀的說明。 |
| 關鍵字 | Nvarchar （*長度*） | 頁面可以篩選之 ad 的類別。 |
| 形成 | int （4） | 指出廣告顯示頻率的數位。 數位愈大，廣告的顯示頻率就愈高。 XML 檔案中所有印象值的總計不得超過 2048000000-1。 |
| 寬度 | int （4） | 影像的寬度（以圖元為單位）。 |
| 高度 | int （4） | 影像的高度（以圖元為單位）。 |

如果您已經有具有不同架構的資料庫，您可以使用**AlternateTextField**、 **ImageUrlField**和**NavigateUrlField**屬性，將 AdRotator 屬性對應至現有的資料庫。 若要在 [AdRotator] 控制項中顯示資料庫的資料，請將資料來源控制項加入至頁面，將資料來源控制項的連接字串設定為指向您的資料庫，並將 [AdRotator] 控制項的 [ **DataSourceID** ] 屬性設為數據源控制項的識別碼。 如果您需要以程式設計方式設定 AdRotator 廣告，請使用 AdCreated 事件。 AdCreated 事件接受兩個參數：一個物件，另一個是 AdCreatedEventArgs 的實例。 AdCreatedEventArgs 是所建立之 ad 的參考。

下列程式碼片段會以程式設計方式設定 ad 的 ImageUrl、NavigateUrl 和 AlternateText：

[!code-csharp[Main](data-bound-controls/samples/sample3.cs)]

## <a name="list-controls"></a>清單控制項

清單控制項包括 ListBox、DropDownList、CheckBoxList、RadioButtonList 和 BulletedList。 這些控制項都可以是系結至資料來源的資料。 它們會使用資料來源中的一個欄位做為顯示文字，而且可以選擇性地使用第二個欄位做為專案的值。 專案也可以在設計階段以靜態方式加入，您可以混合從資料來源加入的靜態專案和動態專案。

若要將清單控制項系結至資料，請將資料來源控制項加入至頁面。 指定資料來源控制項的 [選取] 命令，然後將清單控制項的 [DataSourceID] 屬性設為數據源控制項的識別碼。 使用 [ **DataTextField** ] 和 [ **DataValueField** ] 屬性來定義控制項的顯示文字和值。 此外，您可以使用**DataTextFormatString**屬性來控制顯示文字的外觀，如下所示：

| **運算式** | **說明** |
| --- | --- |
| 價格： {0:C} | 適用于數值/十進位資料。 顯示常值 "Price："，後面接著貨幣格式的數位。 貨幣格式取決於**Page**指示詞或 web.config 檔案中 culture 屬性內所指定的文化特性設定。 |
| {0:D4} | 適用于整數資料。 不能與十進位數搭配使用。 整數會以零填補的欄位顯示，寬度為四個字元。 |
| {0:N2}% | 適用于數值資料。 顯示具有2個小數位數的位數，後面接著常值 "%"。 |
| {0:000.0} | 適用于數值/十進位資料。 數位會舍入到一個小數位數。 小於三位數的數字會填補零。 |
| {0:D} | 適用于日期/時間資料。 顯示完整的日期格式（"星期四，1996年8月6日）。 日期格式取決於頁面或 Web.config 檔案的文化設定。 |
| {0:d} | 適用于日期/時間資料。 顯示簡短日期格式（"12/31/99"）。 |
| {0:yy-MM-dd} | 適用于日期/時間資料。 以數位年-月-日格式顯示日期（96-08-06） |

## <a name="gridview"></a>GridView

GridView 控制項可讓您使用宣告式方法來顯示和編輯表格式資料，而這是 DataGrid 控制項的後繼。 下列功能可在 GridView 控制項中取得。

- 系結至資料來源控制項，例如 SqlDataSource。
- 內建的排序功能。
- 內建的更新和刪除功能。
- 內建分頁功能。
- 內建資料列選取功能。
- 以程式設計方式存取 GridView 物件模型，以動態設定屬性、處理事件等等。
- 多個索引鍵欄位。
- 超連結資料行的多個資料欄位。
- 透過主題和樣式可自訂的外觀。

**資料列欄位**

GridView 控制項中的每個資料行都是由 DataControlField 物件表示。 根據預設，AutoGenerateColumns 屬性會設定為**true**，這會為數據源中的每個欄位建立 AutoGeneratedField 物件。 然後，每個欄位都會轉譯為 GridView 控制項中的資料行，並依照每個欄位出現在資料來源中的順序來呈現。 您也可以將**AutoGenerateColumns**屬性設定為**false** ，然後定義您自己的資料列欄位集合，以手動方式控制**GridView**控制項中顯示的資料列欄位。 不同的資料列欄位類型會決定控制項中資料行的行為。

下表列出可以使用的不同資料列欄位類型。

| **資料列欄位類型** | **說明** |
| --- | --- |
| BoundField | 顯示資料來源中的欄位值。 這是 GridView 控制項的預設資料行類型。 |
| ButtonField | 針對 GridView 控制項中的每個專案顯示一個命令按鈕。 這可讓您建立自訂按鈕控制項的資料行，例如 [加入] 或 [移除] 按鈕。 |
| CheckBoxField | 在 GridView 控制項中顯示每個專案的核取方塊。 此資料列欄位類型通常用來顯示包含布林值的欄位。 |
| CommandField | 顯示預先定義的命令按鈕，以執行選取、編輯或刪除作業。 |
| HyperLinkField | 將資料來源中的欄位值顯示為超連結。 此資料列欄位類型可讓您將第二個欄位系結至超連結的 URL。 |
| ImageField | 針對 GridView 控制項中的每個專案顯示一個影像。 |
| TemplateField | 根據指定的範本，顯示 GridView 控制項中每個專案的使用者定義內容。 此資料列欄位類型可讓您建立自訂資料列欄位。 |

若要以宣告方式定義資料列欄位集合，請先在 GridView 控制項的開頭和結束記號之間加入開頭和結尾 **&lt;資料行&gt;** 標記。 接下來，列出您要在開頭和結尾 **&lt;資料行&gt;** 標記之間包含的資料列欄位。 系統會依照列出的順序，將指定的資料行加入至資料行集合。 **Columns**集合會將所有資料列欄位儲存在控制項中，並可讓您以程式設計方式管理 GridView 控制項中的資料列欄位。

明確宣告的資料列欄位可以與自動產生的資料列欄位一起顯示。 當使用這兩者時，會先轉譯明確宣告的資料列欄位，接著再轉譯自動產生的資料列欄位。

## <a name="binding-to-data"></a>系結至資料

GridView 控制項可以系結至資料來源控制項（例如**SqlDataSource**、 **ObjectDataSource**等），以及任何會執行 system.string 介面的資料來源（例如，System.string、system.web、ArrayList 或 system.string），以及任何會執行此類集合的資料來源。 使用下列其中一種方法，將 GridView 控制項系結至適當的資料來源類型：

- 若要系結至資料來源控制項，請將 GridView 控制項的 DataSourceID 屬性設定為數據源控制項的識別碼值。 GridView 控制項會自動系結至指定的資料來源控制項，而且可以利用資料來源控制項的功能來執行排序、更新、刪除和分頁功能。 這是系結至資料的慣用方法。
- 若要系結至可執行 system.string 介面的資料來源，請以程式設計方式將 GridView 控制項的 DataSource 屬性設定為數據源，然後再呼叫 DataBind 方法。 使用此方法時，GridView 控制項不會提供內建的排序、更新、刪除和分頁功能。 您必須自行提供這種功能。

## <a name="data-operations"></a>資料作業

GridView 控制項提供許多內建功能，可讓使用者排序、更新、刪除、選取和逐頁流覽控制項中的專案。 當 GridView 控制項系結至資料來源控制項時，GridView 控制項可以利用資料來源控制項的功能，並提供自動排序、更新和刪除功能。

> [!NOTE]
> GridView 控制項可以提供對其他資料來源類型進行排序、更新和刪除的支援;不過，您必須為這些作業提供適當的事件處理常式與執行。

排序可讓使用者藉由按一下資料行的標頭，來排序 GridView 控制項中的專案（相對於特定資料行）。 若要啟用排序，請將 AllowSorting 屬性設定為**true**。

按一下 [ **ButtonField** ] 或 [ **TemplateField** ] 資料列欄位中的按鈕時，會啟用自動更新、刪除和選取功能，分別為命令名稱為 [編輯]、[刪除] 和 [選取]。 如果 AutoGenerateEditButton、AutoGenerateDeleteButton 或 AutoGenerateSelectButton 屬性分別設定為**true**，GridView 控制項就可以自動加入具有 [編輯]、[刪除] 或 [選取] 按鈕的**CommandField**資料列欄位。

> [!NOTE]
> GridView 控制項不直接支援將記錄插入資料來源中。 不過，您可以使用 GridView 控制項搭配 DetailsView 或 FormView 控制項來插入記錄。

GridView 控制項不會同時顯示資料來源中的所有記錄，而是會自動將記錄分成多頁。 若要啟用分頁，請將 AllowPaging 屬性設定為**true**。

## <a name="customizing-the-user-interface"></a>自訂使用者介面

您可以針對控制項的不同部分設定樣式屬性，以自訂 GridView 控制項的外觀。 下表列出不同的樣式屬性。

| **Style 屬性** | **說明** |
| --- | --- |
| AlternatingRowStyle | GridView 控制項中交替資料列的樣式設定。 設定此屬性時，會在 RowStyle 設定與**AlternatingRowStyle**設定之間交替顯示資料列。 |
| EditRowStyle | GridView 控制項中所編輯之資料列的樣式設定。 |
| EmptyDataRowStyle | 當資料來源不包含任何記錄時，GridView 控制項中顯示之空白資料列的樣式設定。 |
| FooterStyle | GridView 控制項頁尾資料列的樣式設定。 |
| HeaderStyle | GridView 控制項之標頭資料列的樣式設定。 |
| PagerStyle | GridView 控制項之分頁程式列的樣式設定。 |
| RowStyle | GridView 控制項中資料列的樣式設定。 同時設定**AlternatingRowStyle**屬性時，會在**RowStyle**設定和**AlternatingRowStyle**設定之間交替顯示資料列。 |
| SelectedRowStyle | GridView 控制項中所選取資料列的樣式設定。 |

您也可以顯示或隱藏控制項的不同部分。 下表列出可控制要顯示或隱藏哪些部分的屬性。

| **屬性** | **說明** |
| --- | --- |
| ShowFooter | 顯示或隱藏 GridView 控制項的頁尾區段。 |
| ShowHeader | 顯示或隱藏 GridView 控制項的標頭區段。 |

### <a name="events"></a>事件

GridView 控制項提供幾個您可以對其進行程式設計的事件。 這可讓您在每次發生事件時執行自訂常式。 下表列出 GridView 控制項所支援的事件。

| **Event** | **說明** |
| --- | --- |
| PageIndexChanged | 發生于按一下其中一個分頁程式按鈕時，但在 GridView 控制項處理分頁作業之後。 當您需要在使用者流覽至控制項中的其他頁面之後執行工作時，通常會使用這個事件。 |
| PageIndexChanging | 發生于按一下其中一個分頁程式按鈕時，但在 GridView 控制項處理分頁作業之前。 此事件通常用來取消分頁作業。 |
| RowCancelingEdit | 發生于按一下資料列的 [取消] 按鈕時，但在 GridView 控制項結束編輯模式之前。 這個事件通常用來停止取消作業。 |
| RowCommand | GridView 控制項中按一下按鈕時發生。 當您按一下控制項中的按鈕時，通常會使用這個事件來執行工作。 |
| RowCreated | 在 GridView 控制項中建立新的資料列時發生。 建立資料列時，通常會使用此事件來修改資料列的內容。 |
| RowDataBound | 發生于資料列系結至 GridView 控制項中的資料時。 當資料列系結至資料時，此事件通常用來修改資料列的內容。 |
| RowDeleted | 發生于按一下資料列的 [刪除] 按鈕時，但在 GridView 控制項從資料來源中刪除記錄之後。 這個事件通常用來檢查刪除作業的結果。 |
| RowDeleting | 發生于按一下資料列的 [刪除] 按鈕時，但在 GridView 控制項從資料來源刪除記錄之前。 這個事件通常用來取消刪除作業。 |
| RowEditing | 發生于按一下資料列的 [編輯] 按鈕時，但在 GridView 控制項進入編輯模式之前。 這個事件通常用來取消編輯作業。 |
| RowUpdated | 發生于按一下資料列的 [更新] 按鈕時，但在 GridView 控制項更新資料列之後。 此事件通常用來檢查更新作業的結果。 |
| RowUpdating | 發生于按一下資料列的 [更新] 按鈕時，但在 GridView 控制項更新資料列之前。 這個事件通常用來取消更新作業。 |
| SelectedIndexChanged | 發生于按一下資料列的 [選取] 按鈕時，但在 GridView 控制項處理選取作業之後。 在控制項中選取資料列之後，通常會使用這個事件來執行工作。 |
| SelectedIndexChanging | 發生于按一下資料列的 [選取] 按鈕時，但在 GridView 控制項處理選取作業之前。 這個事件通常用來取消選取作業。 |
| 排序 | 發生于按一下排序資料行的超連結，但 GridView 控制項處理排序作業之後。 這個事件通常用來在使用者按一下超連結以排序資料行之後執行工作。 |
| 排序 | 發生于按一下排序資料行的超連結，但 GridView 控制項處理排序作業之前。 這個事件通常用來取消排序作業或執行自訂排序常式。 |

## <a name="formview"></a>FormView

FormView 控制項是用來顯示資料來源中的單一記錄。 它類似于 DetailsView 控制項，不同之處在于它會顯示使用者定義的範本，而不是資料欄欄位。 建立自己的範本可讓您在控制資料顯示的方式上有更大的彈性。 FormView 控制項支援下列功能：

- 系結至資料來源控制項，例如 SqlDataSource 和 ObjectDataSource。
- 內建的插入功能。
- 內建的更新和刪除功能。
- 內建分頁功能。
- 以程式設計方式存取 FormView 物件模型，以動態設定屬性、處理事件等等。
- 透過使用者定義的範本、主題和樣式，可自訂的外觀。

## <a name="templates"></a>範本

若要讓 FormView 控制項顯示內容，您必須為控制項的不同部分建立範本。 大部分的範本都是選擇性的;不過，您必須為設定控制項的模式建立範本。 例如，支援插入記錄的 FormView 控制項必須定義 [插入專案] 範本。 下表列出您可以建立的不同範本。

| **範本類型** | **說明** |
| --- | --- |
| EditItemTemplate | 當 FormView 控制項處於編輯模式時，定義資料列的內容。 此範本通常包含輸入控制項和命令按鈕，使用者可在其中編輯現有的記錄。 |
| EmptyDataTemplate | 定義當 FormView 控制項系結至不包含任何記錄的資料來源時，所顯示之空白資料列的內容。 此範本通常包含內容，以警示使用者資料來源未包含任何記錄。 |
| FooterTemplate | 定義頁尾資料列的內容。 此範本通常會包含您想要在頁尾資料列中顯示的任何其他內容。 或者，您可以藉由設定 FooterText 屬性，直接指定要在頁尾資料列中顯示的文字。 |
| System.windows.controls.headereditemscontrol.headertemplate | 定義標頭資料列的內容。 此範本通常會包含您想要顯示在標頭資料列中的任何其他內容。 或者，您可以藉由設定 HeaderText 屬性，直接指定要在標頭資料列中顯示的文字。 |
| ItemTemplate | 當 FormView 控制項處於唯讀模式時，定義資料列的內容。 此範本通常包含內容以顯示現有記錄的值。 |
| InsertItemTemplate | 當 FormView 控制項處於插入模式時，定義資料列的內容。 此範本通常包含輸入控制項和命令按鈕，使用者可在其中新增記錄。 |
| PagerTemplate | 定義啟用分頁功能時所顯示之頁面巡覽列的內容（當 AllowPaging 屬性設定為**true**時）。 此範本通常包含使用者可以流覽至另一筆記錄的控制項。 |

您可以使用雙向系結運算式，將 [編輯專案範本] 和 [插入專案] 範本中的輸入控制項系結至資料來源的欄位。 這可讓 FormView 控制項自動將更新或插入作業的輸入控制項值解壓縮。 雙向系結運算式也允許 [編輯專案] 範本中的輸入控制項自動顯示原始域值。

### <a name="binding-to-data"></a>系結至資料

FormView 控制項可以系結至資料來源控制項（例如**SqlDataSource**、AccessDataSource、 **ObjectDataSource**等等），或系結至任何會實作為 system.string 介面的資料來源（例如，System.string、system.web、ArrayList 和 system.string），或。 使用下列其中一種方法，將 FormView 控制項系結至適當的資料來源類型：

- 若要系結至資料來源控制項，請將 FormView 控制項的 DataSourceID 屬性設定為數據源控制項的識別碼值。 FormView 控制項會自動系結至指定的資料來源控制項，而且可以利用資料來源控制項的功能來執行插入、更新、刪除和分頁功能。 這是系結至資料的慣用方法。
- 若要系結至執行**system.string 介面的**資料來源，請以程式設計方式將 FormView 控制項的 DataSource 屬性設定為數據源，然後再呼叫 DataBind 方法。 使用此方法時，FormView 控制項不會提供內建的插入、更新、刪除和分頁功能。 您需要使用適當的事件來提供這項功能。

## <a name="data-operations"></a>資料作業

FormView 控制項提供許多內建功能，可讓使用者更新、刪除、插入及逐頁流覽控制項中的專案。 當 FormView 控制項系結至資料來源控制項時，FormView 控制項可以利用資料來源控制項的功能，並提供自動更新、刪除、插入和分頁功能。 FormView 控制項可以提供與其他資料來源類型之更新、刪除、插入和分頁作業的支援;不過，您必須為這些作業提供適當的事件處理常式與執行。

因為 FormView 控制項使用範本，所以不會提供自動產生命令按鈕以執行更新、刪除或插入作業的方式。 您必須以手動方式將這些命令按鈕包含在適當的範本中。 FormView 控制項可辨識將其**CommandName**屬性設定為特定值的特定按鈕。 下表列出 FormView 控制項可識別的命令按鈕。

| **Button** | **Commandname 值** | **說明** |
| --- | --- | --- |
| 取消 | 退出 | 用於更新或插入作業，以取消操作並捨棄使用者輸入的值。 FormView 控制項接著會回到 DefaultMode 屬性所指定的模式。 |
| 刪除 | "Delete" | 用於刪除作業，以從資料來源刪除顯示的記錄。 引發 ItemDeleting 和 ItemDeleted 事件。 |
| 編輯 | 編輯 | 用於更新作業，將 FormView 控制項放在編輯模式中。 **EditItemTemplate**屬性中指定的內容會顯示在資料列中。 |
| 插入 | 插入 | 用於插入作業，以嘗試使用使用者所提供的值，在資料來源中插入新記錄。 引發 ItemInserting 和 ItemInserted 事件。 |
| 新增 | 新 | 用於插入作業，將 FormView 控制項放入插入模式。 **InsertItemTemplate**屬性中指定的內容會顯示在資料列中。 |
| 頁面 | 本頁 | 在分頁作業中用來代表執行分頁的頁面巡覽列中的按鈕。 若要指定分頁作業，請將按鈕的**CommandArgument**屬性設定為 [下一步]、[上一步]、[第一個]、[最後一個]，或要流覽的頁面索引。 引發 PageIndexChanging 和 PageIndexChanged 事件。 |
| 更新 | 更新 | 用於更新作業，嘗試使用使用者所提供的值來更新資料來源中顯示的記錄。 引發 ItemUpdating 和 ItemUpdated 事件。 |

不同于 [刪除] 按鈕（這會立即刪除顯示的記錄），按一下 [編輯] 或 [新增] 按鈕時，FormView 控制項會分別進入 [編輯] 或 [插入] 模式。 在編輯模式中，會顯示目前資料項目的**EditItemTemplate**屬性所包含的內容。 通常會定義 [編輯專案] 範本，讓 [編輯] 按鈕取代為 [更新] 和 [取消] 按鈕。 欄位的資料類型（例如 TextBox 或 CheckBox 控制項）適用的輸入控制項，通常也會顯示欄位的值供使用者修改。 按一下 [更新] 按鈕會更新資料來源中的記錄，而按一下 [取消] 按鈕會放棄任何變更。

同樣地，當控制項處於插入模式時，[ **InsertItemTemplate** ] 屬性中所包含的內容也會顯示為數據項。 [插入專案] 範本的定義通常是讓 [新增] 按鈕取代為 [插入] 和 [取消] 按鈕，並顯示空白的輸入控制項，讓使用者輸入新記錄的值。 按一下 [插入] 按鈕會將記錄插入資料來源中，而按一下 [取消] 按鈕會放棄任何變更。

FormView 控制項提供分頁功能，可讓使用者流覽至資料來源中的其他記錄。 啟用時，會在包含頁面導覽控制項的 FormView 控制項中顯示呼機資料列。 若要啟用分頁，請將**AllowPaging**屬性設定為**true**。 您可以藉由設定包含在 PagerStyle 和 PagerSettings 屬性中之物件的屬性來自訂頁面巡覽列。 您可以使用**PagerTemplate**屬性來建立自己的 ui，而不是使用內建的分頁資料列 ui。

## <a name="customizing-the-user-interface"></a>自訂使用者介面

您可以針對控制項的不同部分設定樣式屬性，以自訂 FormView 控制項的外觀。 下表列出不同的樣式屬性。

| **Style 屬性** | **說明** |
| --- | --- |
| EditRowStyle | 當 FormView 控制項處於編輯模式時，資料列的樣式設定。 |
| EmptyDataRowStyle | 當資料來源不包含任何記錄時，在 FormView 控制項中顯示之空白資料列的樣式設定。 |
| FooterStyle | FormView 控制項之頁尾資料列的樣式設定。 |
| HeaderStyle | FormView 控制項之標頭資料列的樣式設定。 |
| InsertRowStyle | 當 FormView 控制項處於插入模式時，資料列的樣式設定。 |
| PagerStyle | 啟用分頁功能時，在 FormView 控制項中顯示之分頁資料列的樣式設定。 |
| RowStyle | 當 FormView 控制項為唯讀模式時，資料列的樣式設定。 |

## <a name="events"></a>事件

FormView 控制項提供幾個您可以針對進行程式設計的事件。 這可讓您在每次發生事件時執行自訂常式。 下表列出 FormView 控制項所支援的事件。

| **Event** | **說明** |
| --- | --- |
| ItemCommand | 按一下 FormView 控制項內的按鈕時發生。 當您按一下控制項中的按鈕時，通常會使用這個事件來執行工作。 |
| ItemCreated | 在 FormView 控制項中建立所有 FormViewRow 物件之後發生。 此事件通常用來在顯示記錄之前修改其值。 |
| ItemDeleted | 當按一下 [刪除] 按鈕（其**CommandName**屬性設定為 "Delete" 的按鈕）時發生，但在 FormView 控制項刪除資料來源中的記錄之後。 這個事件通常用來檢查刪除作業的結果。 |
| ItemDeleting | 發生于按一下 [刪除] 按鈕時，但在 FormView 控制項從資料來源刪除記錄之前。 這個事件通常用來取消刪除作業。 |
| ItemInserted | 發生于按一下 [插入] 按鈕（其**CommandName**屬性設定為 "Insert" 的按鈕）時，但在 FormView 控制項插入記錄之後。 這個事件通常用來檢查插入作業的結果。 |
| ItemInserting | 發生于按一下 [插入] 按鈕時，但在 FormView 控制項插入記錄之前。 這個事件通常用來取消插入作業。 |
| ItemUpdated | 發生于按一下 [更新] 按鈕（其**CommandName**屬性設定為 "Update" 的按鈕）時，但在 FormView 控制項更新資料列之後。 此事件通常用來檢查更新作業的結果。 |
| ItemUpdating | 發生于按一下 [更新] 按鈕時，但在 FormView 控制項更新記錄之前。 此事件通常用來取消更新作業。 |
| ModeChanged | 在 FormView 控制項變更模式（以編輯、插入或唯讀模式）之後發生。 當 FormView 控制項變更模式時，通常會使用這個事件來執行工作。 |
| ModeChanging | 在 FormView 控制項變更模式之前發生（以編輯、插入或唯讀模式）。 這個事件通常用來取消模式變更。 |
| PageIndexChanged | 發生于按一下其中一個分頁程式按鈕時，但在 FormView 控制項處理分頁作業之後。 當您需要在使用者導覽至控制項中的不同記錄之後執行工作時，通常會使用這個事件。 |
| PageIndexChanging | 發生于按一下其中一個分頁程式按鈕時，但在 FormView 控制項處理分頁作業之前。 此事件通常用來取消分頁作業。 |

## <a name="detailsview"></a>DetailsView

DetailsView 控制項是用來顯示資料表中資料來源的單一記錄，其中記錄的每個欄位都會顯示在資料表的一列中。 它可以與 GridView 控制項搭配使用，以取得主要詳細資料案例。 DetailsView 控制項支援下列功能：

- 系結至資料來源控制項，例如 SqlDataSource。
- 內建的插入功能。
- 內建的更新和刪除功能。
- 內建分頁功能。
- 以程式設計方式存取 DetailsView 物件模型，以動態設定屬性、處理事件等等。
- 透過主題和樣式可自訂的外觀。

## <a name="row-fields"></a>資料欄欄位

DetailsView 控制項中的每個資料列都是藉由宣告欄位控制項來建立。 不同的資料欄欄位類型決定了控制項中資料列的行為。 欄位控制項衍生自 DataControlField。 下表列出可以使用的不同資料欄欄位類型。

| **資料列欄位類型** | **說明** |
| --- | --- |
| BoundField | 將資料來源中的欄位值顯示為文字。 |
| ButtonField | 在 DetailsView 控制項中顯示命令按鈕。 這可讓您顯示具有自訂按鈕控制項的資料列，例如 [加入] 或 [移除] 按鈕。 |
| CheckBoxField | 在 DetailsView 控制項中顯示覆選框。 此資料欄欄位類型通常用來顯示包含布林值的欄位。 |
| CommandField | 顯示內建命令按鈕，以在 DetailsView 控制項中執行編輯、插入或刪除作業。 |
| HyperLinkField | 將資料來源中的欄位值顯示為超連結。 此資料欄欄位類型可讓您將第二個欄位系結至超連結的 URL。 |
| ImageField | 在 DetailsView 控制項中顯示影像。 |
| TemplateField | 根據指定的範本，顯示 DetailsView 控制項中資料列的使用者定義內容。 此資料欄欄位類型可讓您建立自訂的資料欄欄位。 |

根據預設，AutoGenerateRows 屬性會設定為**true**，它會針對資料來源中可系結類型的每個欄位，自動產生系結的資料欄欄位物件。 有效的可系結類型為 String、DateTime、Decimal、Guid 和一組基本型別。 然後每個欄位都會以文字的形式顯示在資料列中，而每個欄位的順序會依每個欄位出現在資料來源中。

自動產生資料列提供快速且簡單的方式來顯示記錄中的每個欄位。 不過，若要利用 DetailsView 控制項的先進功能，您必須明確宣告要包含在 DetailsView 控制項中的資料欄欄位。 若要宣告資料欄欄位，請先將**AutoGenerateRows**屬性設定為**false**。 接下來，在 DetailsView 控制項的開頭和結束記號之間加入開頭和結尾 **&lt;欄位&gt;** 標記。 最後，列出您要包含在開頭和結尾 **&lt;欄位&gt;** 標記之間的資料欄欄位。 所指定的資料欄欄位會依照列出的順序加入 [欄位] 集合中。 **Fields**集合可讓您以程式設計方式管理 DetailsView 控制項中的資料欄欄位。

> [!NOTE]
> 自動產生的資料欄欄位不會加入至 Fields 集合。

## <a name="binding-to-data"></a>系結至資料

DetailsView 控制項可以系結至資料來源控制項（例如**SqlDataSource**或 AccessDataSource），或系結至任何可執行 system.string 介面的資料來源，例如 System.web、system.web、System.string 和 System.object。 Hashtable。

使用下列其中一種方法，將 DetailsView 控制項系結至適當的資料來源類型：

- 若要系結至資料來源控制項，請將 DetailsView 控制項的 DataSourceID 屬性設定為數據源控制項的識別碼值。 DetailsView 控制項會自動系結至指定的資料來源控制項。 這是系結至資料的慣用方法。
- 若要系結至可執行**system.string 介面的**資料來源，請以程式設計方式將 DetailsView 控制項的 DataSource 屬性設定為數據源，然後再呼叫 DataBind 方法。

## <a name="security"></a>安全性

這個控制項可以用來顯示使用者輸入，其中可能包含惡意的用戶端腳本。 在應用程式中顯示之前，檢查是否有任何從用戶端針對可執行檔腳本、SQL 語句或其他程式碼傳送的資訊。 ASP.NET 提供輸入要求驗證功能，以封鎖使用者輸入中的腳本和 HTML。

## <a name="data-operations"></a>資料作業

DetailsView 控制項提供內建功能，可讓使用者更新、刪除、插入及逐頁流覽控制項中的專案。 當 DetailsView 控制項系結至資料來源控制項時，DetailsView 控制項可以利用資料來源控制項的功能，並提供自動更新、刪除、插入和分頁功能。

DetailsView 控制項可以提供與其他資料來源類型之更新、刪除、插入和分頁作業的支援;不過，您必須在適當的事件處理常式中提供這些作業的執行。

DetailsView 控制項可以將 AutoGenerateEditButton、AutoGenerateDeleteButton 或 AutoGenerateInsertButton 屬性分別設定為**true**，以自動加入具有 [編輯]、[刪除] 或 [新增] 按鈕的**CommandField**資料欄欄位。 與 [刪除] 按鈕（會立即刪除選取的記錄）不同的是，按一下 [編輯] 或 [新增] 按鈕時，DetailsView 控制項會分別進入編輯或插入模式。 在編輯模式中，[編輯] 按鈕會取代為 [更新] 和 [取消] 按鈕。 欄位的資料類型（例如 TextBox 或 CheckBox 控制項）適用的輸入控制項，會顯示欄位的值供使用者修改。 按一下 [更新] 按鈕會更新資料來源中的記錄，而按一下 [取消] 按鈕會放棄任何變更。 同樣地，在插入模式中，會使用 [插入] 和 [取消] 按鈕取代 [新增] 按鈕，並顯示空的輸入控制項，讓使用者輸入新記錄的值。

DetailsView 控制項提供分頁功能，可讓使用者流覽至資料來源中的其他記錄。 啟用時，頁面導覽控制項就會顯示在 [呼機] 資料列中。 若要啟用分頁，請將 AllowPaging 屬性設定為**true**。 您可以使用 PagerStyle 和 PagerSettings 屬性來自訂頁面巡覽列。

## <a name="customizing-the-user-interface"></a>自訂使用者介面

您可以針對控制項的不同部分設定樣式屬性，以自訂 DetailsView 控制項的外觀。 下表列出不同的樣式屬性。

| **Style 屬性** | **說明** |
| --- | --- |
| AlternatingRowStyle | DetailsView 控制項中交替資料列的樣式設定。 設定此屬性時，會在 RowStyle 設定與**AlternatingRowStyle**設定之間交替顯示資料列。 |
| CommandRowStyle | 資料列的樣式設定，其中包含 DetailsView 控制項中的內建命令按鈕。 |
| EditRowStyle | 當 DetailsView 控制項處於編輯模式時，資料列的樣式設定。 |
| EmptyDataRowStyle | 當資料來源不包含任何記錄時，在 DetailsView 控制項中顯示之空白資料列的樣式設定。 |
| FooterStyle | DetailsView 控制項之頁尾資料列的樣式設定。 |
| HeaderStyle | DetailsView 控制項之標頭資料列的樣式設定。 |
| InsertRowStyle | 當 DetailsView 控制項處於插入模式時，資料列的樣式設定。 |
| PagerStyle | DetailsView 控制項之分頁資料列的樣式設定。 |
| RowStyle | DetailsView 控制項中資料列的樣式設定。 同時設定**AlternatingRowStyle**屬性時，會在**RowStyle**設定和**AlternatingRowStyle**設定之間交替顯示資料列。 |
| FieldHeaderStyle | DetailsView 控制項之標頭資料行的樣式設定。 |

## <a name="events"></a>事件

DetailsView 控制項提供幾個您可以對其進行程式設計的事件。 這可讓您在每次發生事件時執行自訂常式。 下表列出 DetailsView 控制項所支援的事件。 DetailsView 控制項也會從其基類繼承這些事件：資料系結、資料系結、處置、初始化、載入、呈現和轉譯。

| **Event** | **說明** |
| --- | --- |
| ItemCommand | 發生于在 DetailsView 控制項中按一下按鈕時。 |
| ItemCreated | 在 DetailsView 控制項中建立所有 DetailsViewRow 物件之後發生。 此事件通常用來在顯示記錄之前修改其值。 |
| ItemDeleted | 發生于按一下 [刪除] 按鈕時，但在 DetailsView 控制項從資料來源刪除記錄之後。 這個事件通常用來檢查刪除作業的結果。 |
| ItemDeleting | 發生于按一下 [刪除] 按鈕時，但在 DetailsView 控制項從資料來源刪除記錄之前。 這個事件通常用來取消刪除作業。 |
| ItemInserted | 發生于按一下 [插入] 按鈕時，但在 DetailsView 控制項插入記錄之後。 這個事件通常用來檢查插入作業的結果。 |
| ItemInserting | 發生于按一下 [插入] 按鈕時，但在 DetailsView 控制項插入記錄之前。 這個事件通常用來取消插入作業。 |
| ItemUpdated | 發生于按一下 [更新] 按鈕時，但在 DetailsView 控制項更新資料列之後。 此事件通常用來檢查更新作業的結果。 |
| ItemUpdating | 發生于按一下 [更新] 按鈕時，但在 DetailsView 控制項更新記錄之前。 此事件通常用來取消更新作業。 |
| ModeChanged | 在 DetailsView 控制項變更模式（[編輯]、[插入] 或 [唯讀] 模式）之後發生。 當 DetailsView 控制項變更模式時，通常會使用此事件來執行工作。 |
| ModeChanging | 發生于 DetailsView 控制項變更模式之前（編輯、插入或唯讀模式）。 這個事件通常用來取消模式變更。 |
| PageIndexChanged | 發生于按一下其中一個分頁程式按鈕時，但在 DetailsView 控制項處理分頁作業之後。 當您需要在使用者導覽至控制項中的不同記錄之後執行工作時，通常會使用這個事件。 |
| PageIndexChanging | 發生于按一下其中一個分頁程式按鈕時，但在 DetailsView 控制項處理分頁作業之前。 此事件通常用來取消分頁作業。 |

## <a name="the-menu-control"></a>Menu 控制項

ASP.NET 2.0 中的 Menu 控制項是設計成功能完整的導覽系統。 它可以輕鬆地資料系結至階層式資料來源，例如 SiteMapDataSource。

功能表控制項結構可以宣告式或動態方式定義，而且是由單一根節點和任意數目的子節點所組成。 下列程式碼會以宣告方式定義 Menu 控制項的功能表。

[!code-aspx[Main](data-bound-controls/samples/sample4.aspx)]

在上述範例中，首頁 .aspx 節點是根節點。 所有其他節點都是在不同層級的根節點內加以嵌套。

功能表控制項可以呈現兩種類型的功能表;靜態功能表和動態功能表。 靜態功能表是由永遠可見的功能表項目所組成。 動態功能表包含只有當使用者以滑鼠停留在其上時才會顯示的功能表項目。 客戶通常會混淆靜態功能表與以宣告方式定義的功能表，以及具有在執行時間進行資料系結之功能表的動態功能表。 事實上，動態和靜態功能表與擴充方法無關。 「*靜態*」和「*動態*」這一詞只會參考功能表是否預設為靜態顯示，還是只有在使用者採取一些動作時才會顯示。

**StaticDisplayLevels**屬性是用來設定有多少層級的功能表是靜態的，因此預設會顯示。 在上述範例中，將**StaticDisplayLevels**屬性設定為2的值，會導致功能表以靜態方式顯示 [主節點]、[音樂] 節點和 [電影] 節點。 當使用者將滑鼠停留在父節點上時，會動態顯示所有其他節點。

**MaximumDynamicDisplayLevels**屬性會設定功能表能夠顯示的動態層級數目上限。 任何層級高於**MaximumDynamicDisplayLevels**屬性所指定之值的動態功能表都會遭到捨棄。

> [!NOTE]
> 在某些情況下，您可能會遇到因為 MaximumDynamicDisplayLevels 屬性而不會呈現功能表的情況。 在這些情況下，請確定屬性的設定足以允許顯示 [客戶] 功能表。

## <a name="data-binding-the-menu-control"></a>資料系結功能表控制項

Menu 控制項可以系結至任何階層式資料來源，例如 SiteMapDataSource 或 XMLDataSource。 SiteMapDataSource 是最常使用的方法，可將資料系結至 Menu 控制項，因為它會從 web.sitemap 檔案中饋送，而其架構會提供已知的 API 給功能表控制項。 下列清單顯示簡單的網站地圖檔案。

[!code-xml[Main](data-bound-controls/samples/sample5.xml)]

請注意，只有一個根 siteMapNode 元素，在此案例中為 Home 元素。 可以為每個 siteMapNode 設定數個屬性。 最常使用的屬性為：

- **url**指定使用者按一下功能表項目時所要顯示的 URL。 如果此屬性不存在，則節點會在按下時，直接回傳。
- **標題**指定顯示在功能表項目上的文字。
- **描述**當做節點的檔使用。 當滑鼠停留在節點上時，也會顯示為工具提示。
- **siteMapFile**允許嵌套網站地圖。 這個屬性必須指向格式正確的 ASP.NET 網站地圖檔案。
- **角色**允許透過 ASP.NET 安全性調整來控制節點的外觀。

請注意，雖然這些屬性都是選擇性的，但如果未指定，則功能表的行為可能不是預期的行為。 例如，如果指定了*url*屬性，但*description*屬性不是，則不會顯示該節點，而且將無法流覽至指定的 url。

## <a name="controlling-a-menus-operation"></a>控制功能表作業

有數個屬性會影響 ASP.NET 功能表控制項的作業;**取向**屬性、 **DisappearAfter**屬性、 **StaticItemFormatString**屬性和**StaticPopoutImageUrl**屬性只是其中的其中一個。

- **方向**可以設定為*水準*或*垂直*，並控制靜態功能表項目是在資料列中水準配置，還是以垂直方式堆疊在另一個。 這個屬性不會影響動態功能表。
- **DisappearAfter**屬性會設定動態功能表在移動滑鼠之後應該保持可見的時間長度。 此值以毫秒為單位指定，預設值為500。 將此屬性設定為-1 的值，會導致功能表永遠不會自動消失。 在這種情況下，功能表只會在使用者按一下功能表外部時消失。
- **StaticItemFormatString**屬性可讓您輕鬆地在功能表系統中維護一致的措辭。 指定此屬性時，應該輸入 *{0}* ，以取代出現在資料來源中的描述。 例如，若要讓練習1中的功能表項目說一下 [造訪我們的產品] 頁面，您可以指定 [流覽我們的 {0}] 頁面以取得 StaticItemFormatString。 在執行時間，ASP.NET 會將任何出現的 {0} 取代為功能表項目的正確描述。
- **StaticPopoutImageUrl**屬性會指定用來指出特定功能表節點具有子節點的影像，您可以將滑鼠暫留在其上。 動態功能表會繼續使用預設影像。

## <a name="templated-menu-controls"></a>樣板化功能表控制項

Menu 控制項是樣板化控制項，允許兩個不同的 ItemTemplates;StaticItemTemplate 和 DynamicItemTemplate。 使用這些範本，您可以輕鬆地將伺服器控制項或使用者控制項加入至功能表。

若要在 Visual Studio .NET 中編輯範本，請按一下功能表上的 [智慧標籤] 按鈕，然後選擇 [編輯範本]。 接著，您可以在編輯 StaticItemTemplate 或 DynamicItemTemplate 之間選擇。

加入至 StaticItemTemplate 的任何控制項都會在頁面載入時出現在靜態功能表中。 新增至 DynamicItemTemplate 的任何控制項都會出現在所有快顯功能表上。

## <a name="menu-events"></a>功能表事件

Menu 控制項有兩個唯一的事件;**MenuItemClicked**和**MenuItemDatabound**事件。

當您按一下功能表項目時，就會引發 MenuItemClicked 事件。 當功能表項目是資料系結時，就會引發 MenuItemDatabound 事件。 傳遞至事件處理常式的**MenuEventArgs** ，可透過 item 屬性提供功能表項目的存取權。

## <a name="controlling-a-menus-appearance"></a>控制功能表外觀

您也可以使用可用於格式化功能表的一或多個樣式，來影響功能表控制項的外觀。 其中包括**StaticMenuStyle**、 **DynamicMenuStyle**、 **DynamicMenuItemStyle**、 **DynamicSelectedStyle**和**DynamicHoverStyle**。 這些屬性是使用標準 HTML 樣式字串進行設定。 例如，下列程式會影響動態功能表的樣式。

[!code-aspx[Main](data-bound-controls/samples/sample6.aspx)]

> [!NOTE]
> 如果您使用任何暫留樣式，您必須將 &lt;head&gt; 元素加入至頁面，並將*runat*元素設為*server*。

Menu 控制項也支援使用 ASP.NET 2.0 主題。

## <a name="the-treeview-control"></a>TreeView 控制項

TreeView 控制項會以類似樹狀結構的方式來顯示資料。 與 Menu 控制項一樣，它可以輕鬆地系結至任何階層式資料來源，例如 SiteMapDataSource。

客戶可能會在 ASP.NET 2.0 中詢問 TreeView 控制項的第一個問題，就是它是否與適用于 ASP.NET 1.x 的 TreeView IE WebControl 相關。 不是。 ASP.NET 2.0 TreeView 控制項是從頭開始撰寫，並針對先前提供的 IE TreeView WebControl 提供顯著的改善。

我不會詳細說明如何將 TreeView 控制項系結到網站地圖，因為它的執行方式與 Menu 控制項完全相同。 不過，TreeView 控制項的運作方式有一些不同的差異。

根據預設，TreeView 控制項會完全展開。 若要在初始載入時變更擴充層級，請修改控制項的**ExpandDepth**屬性。 當 TreeView 在展開特定節點時進行資料系結時，這一點特別重要。

## <a name="databinding-the-treeview-control"></a>資料系結 TreeView 控制項

與 Menu 控制項不同的是，TreeView 很適合處理大量資料。 因此，除了資料系結至 SiteMapDataSource 或 XMLDataSource 以外，TreeView 通常也會系結至資料集或其他關聯式資料。 在 TreeView 控制項系結至大量資料的情況下，最好只系結至實際顯示在控制項中的資料。 接著，您可以將資料系結至其他資料，因為 TreeView 節點會展開。

在這些情況下，TreeView 的**PopulateOnDemand**屬性應該設定為*true*。 接著，您必須提供**TreeNodePopulate**方法的執行。

## <a name="data-binding-without-postback"></a>不回傳的資料系結

請注意，當您第一次展開上一個範例中的節點時，頁面會回傳並重新整理。 在此範例中，這不是問題，但您可以想像它可能在具有大量資料的生產環境中。 較佳的案例是，TreeView 仍會動態填入其節點，但不會回傳至伺服器。

藉由將**PopulateNodesFromClient**和**PopulateOnDemand**屬性設定為 true，ASP.NET TreeView 控制項會以動態方式填入節點，而不會回傳。 當父節點展開時，會從用戶端發出 XMLHttp 要求，並引發 OnTreeNodePopulate 事件。 伺服器會以 XML 資料島回應，然後用來系結子節點的資料。

ASP.NET 會動態建立可執行此功能的用戶端程式代碼。 包含腳本&gt; 標記的 &lt;腳本會產生指向 AXD 檔案。 例如，下列清單顯示產生 XMLHttp 要求之腳本的腳本連結。

[!code-html[Main](data-bound-controls/samples/sample7.html)]

如果您在瀏覽器中流覽此 AXD 檔案並加以開啟，您會看到可執行 XMLHttp 要求的程式碼。 這個方法可防止客戶修改腳本檔案。

## <a name="controlling-the-operation-of-the-treeview-control"></a>控制 TreeView 控制項的作業

TreeView 控制項有數個會影響控制項作業的屬性。 最明顯的屬性包括**ShowCheckBoxes**、 **ShowExpandCollapse**和**ShowLines**。

**ShowCheckBoxes**屬性會影響節點在轉譯時是否顯示覆選框。 這個屬性的有效值為**None**、 **Root**、 **Parent**、分**葉**和**All**。 這些會影響 TreeView 控制項，如下所示：

| **屬性值** | **Effect** |
| --- | --- |
| 無 | 核取方塊不會顯示在任何節點上。 這是預設設定。 |
| Root | 核取方塊只會顯示在根節點上。 |
| 父代 | 只有在具有子節點的節點上才會顯示覆選框。 這些子節點可以是父節點或分葉節點。 |
| 分葉 | 只有在沒有子節點的節點上才會顯示覆選框。 |
| 全部： | 所有節點上都會顯示一個核取方塊。 |

當使用核取方塊時， **CheckedNodes**屬性會傳回在回傳時檢查的 TreeView 節點集合。

**ShowExpandCollapse**屬性控制 [根] 和 [父節點] 旁展開/折迭影像的外觀。 如果此屬性設定為**false**，TreeView 節點會轉譯為超連結，並藉由按一下連結來展開/折迭。

**ShowLines**屬性可控制是否顯示將父節點連接到子節點的行。 若**為 false** （預設值），則不會顯示任何行。 若**為 true**，TreeView 控制項將會使用**LineImagesFolder**屬性所指定之資料夾中的線條影像。

若要自訂 TreeView 線條的外觀，Visual Studio .NET 2005 包含一行設計工具。 您可以使用 TreeView 控制項上的 [智慧標籤] 按鈕來存取此工具，如下所示。

![](data-bound-controls/_static/image1.jpg)

**圖 1**

當您選取 [**自訂線條影像**] 功能表選項時，將會啟動 [程式列設計師] 工具，讓您設定 TreeView 線條的外觀。

## <a name="treeview-events"></a>TreeView 事件

TreeView 控制項具有下列唯一的事件：

- 根據**SelectAction**屬性選取節點時，就會發生 SelectedNodeChanged。
- 當節點 checkboxs 狀態變更時，就會發生 TreeNodeCheckChanged。
- 當節點根據**SelectAction**屬性展開時，就會發生 TreeNodeExpanded。
- 當節點折迭時，就會發生 TreeNodeCollapsed。
- 當節點為數據系結時，就會發生 TreeNodeDataBound。
- 當節點已填入時，就會發生 TreeNodePopulate。

**SelectAction**屬性可讓您設定在選取節點時所引發的事件。 SelectAction 屬性提供下列動作：

- TreeNodeSelectAction。當選取節點時，請展開 [引發 TreeNodeExpanded]。
- TreeNodeSelectAction。當選取節點時，None 不會引發任何事件。
- TreeNodeSelectAction：選取此節點時，會引發 SelectedNodeChanged 事件。
- TreeNodeSelectAction： SelectExpand 會在選取節點時，同時引發 SelectedNodeChanged 事件和 TreeNodeExpanded 事件。

## <a name="controlling-appearance-with-styles"></a>使用樣式控制外觀

TreeView 控制項提供許多屬性，以控制樣式的控制面板。 有下列屬性可供使用。

| **屬性名稱** | **控制項** |
| --- | --- |
| HoverNodeStyle | 控制當滑鼠停留在其上時，節點的樣式。 |
| LeafNodeStyle | 控制分葉節點的樣式。 |
| NodeStyle | 控制所有節點的樣式。 特定節點樣式（例如 LeafNodeStyle）會覆寫此樣式。 |
| ParentNodeStyle | 控制所有父節點的樣式。 |
| RootNodeStyle | 控制根節點的樣式。 |
| SelectedNodeStyle | 控制所選節點的樣式。 |

這些屬性都是唯讀的。 不過，它們會每個傳回一個**TreeNodeStyle**物件，而該物件的屬性可以使用*屬性-子屬性*格式來修改。 例如，若要設定**SelectedNodeStyle**的**前景**屬性，您可以使用下列語法：

[!code-aspx[Main](data-bound-controls/samples/sample8.aspx)]

請注意，上面的標記不會關閉。 這是因為在使用此處所示的宣告式語法時，您也會在 HTML 程式碼中包含 Treeview 節點。

您也可以在程式碼中使用*屬性（property* ），指定樣式屬性。 例如，若要在程式碼中設定**RootNodeStyle**的**前景**屬性，您可以使用下列語法：

[!code-csharp[Main](data-bound-controls/samples/sample9.cs)]

> [!NOTE]
> 如需不同樣式屬性的完整清單，請參閱 MSDN 檔中有關 TreeNodeStyle 物件的資訊。

## <a name="the-sitemappath-control"></a>SiteMapPath 控制項

SiteMapPath 控制項提供 ASP.NET 開發人員的階層連結導覽控制項。 就像其他導覽控制項一樣，也可以輕鬆地將資料系結至階層式資料來源，例如 SiteMapDataSource 或 XmlDataSource。

SiteMapPath 控制項是由 SiteMapNodeItem 物件所組成。 節點有三種類型：根節點、父節點，以及目前的節點。 根節點是階層式結構頂端的節點。 目前的節點代表目前的頁面。 所有其他節點都是父節點。

## <a name="controlling-the-operation-of-the-sitemappath-control"></a>控制 SiteMapPath 控制項的操作

控制 SiteMapPath 控制項操作的屬性如下所示：

| **屬性** | **屬性的描述** |
| --- | --- |
| ParentLevelsDisplayed | 控制要顯示多少個父節點。 預設值為-1，對顯示的父節點數目不會有任何限制。 |
| PathDirection | 控制 SiteMapPath 的方向。 有效值為 RootToCurrent （預設值）和 CurrentToRoot。 |
| PathSeparator | 控制在 SiteMapPath 控制項中分隔節點之字元的字串。 預設值為：。 |
| RenderCurrentNodeAsLink | 布林值，控制目前的節點是否呈現為連結。 預設值是 False。 |
| SkipLinkText | 當螢幕閱讀程式看到頁面時，協助協助工具。 此屬性允許螢幕閱讀者略過 SiteMapPath 控制項。 若要停用此功能，請將屬性設定為字串. 空白。 |

## <a name="templated-sitemappath-controls"></a>樣板化 SiteMapPath 控制項

SiteMapControl 是樣板化控制項，因此您可以定義不同的範本，以用於顯示控制項。 若要編輯 SiteMapPath 控制項中的範本，請按一下控制項上的 [智慧標籤] 按鈕，然後從功能表中選擇 [編輯範本]。 這會顯示 [SiteMapTasks] 功能表，如下所示，您可以在可用的不同範本之間進行選擇。

![](data-bound-controls/_static/image2.jpg)

**圖 2**

**NodeTemplate**範本是指 SiteMapPath 中的任何節點。 如果節點是根節點或目前節點，而且已設定**RootNodeTemplate**或**CurrentNodeTemplate** ，則會覆寫 NodeTemplate。

## <a name="sitemappath-events"></a>SiteMapPath 事件

SiteMapPath 控制項有兩個不是衍生自 Control 類別的事件;**ItemCreated**事件和**ItemDataBound**事件。 建立 SiteMapPath 專案時，會引發 ItemCreated 事件。 在 SiteMapPath-節點的資料系結期間呼叫 DataBind 方法時，會引發 ItemDataBound。 **SiteMapNodeItemEventArgs**物件可讓您透過 Item 屬性存取特定的 SiteMapNodeItem。

## <a name="controlling-appearance-with-styles"></a>使用樣式控制外觀

下列樣式適用于 SiteMapPath 控制項的格式設定。

| **屬性名稱** | **控制項** |
| --- | --- |
| CurrentNodeStyle | 控制目前節點的文字樣式。 |
| RootNodeStyle | 控制根節點的文字樣式。 |
| NodeStyle | 控制所有節點的文字樣式，假設不適用 CurrentNodeStyle 或 RootNodeStyle。 |

CurrentNodeStyle 或 RootNodeStyle 會覆寫 NodeStyle 屬性。 這些屬性都是唯讀的，並會傳回**Style**物件。 若要使用其中一個屬性來影響節點的外觀，您必須設定所傳回樣式物件的屬性。 例如，下列程式碼會變更目前節點的前景屬性。

[!code-aspx[Main](data-bound-controls/samples/sample10.aspx)]

屬性也可以透過程式設計方式套用，如下所示：

[!code-csharp[Main](data-bound-controls/samples/sample11.cs)]

> [!NOTE]
> 如果套用了範本，將不會套用樣式。

## <a name="lab-1-configuring-an-aspnet-menu-control"></a>實驗室1：設定 ASP.NET 功能表控制項

1. 建立新網站。
2. 選取 [檔案]、[新增]、[檔案]，然後從檔案範本清單中選擇 [網站地圖]，以新增網站地圖檔案。
3. 開啟網站地圖（預設為網站地圖）並加以修改，使其看起來像下面的清單。 您在網站地圖檔案中連結的頁面並不存在，但這在此練習中不會有問題。

    [!code-xml[Main](data-bound-controls/samples/sample12.xml)]
4. 在設計檢視中開啟預設 Web 表單。
5. 從 [工具箱] 的導覽區段中，將新的 [Menu] 控制項加入至頁面。
6. 從 [工具箱] 的 [資料] 區段中，加入新的 SiteMapDataSource。 SiteMapDataSource 會自動在您的網站中使用網站地圖檔案。 （Web.config 檔案*必須*位於網站的根資料夾中）。
7. 按一下 [Menu] 控制項，然後按一下 [智慧標籤] 按鈕以顯示 [功能表工作] 對話方塊。
8. 在 [選擇資料來源] 下拉式清單中，選取 [SiteMapDataSource1]。
9. 按一下 [自動格式設定] 連結，然後選擇功能表的格式。
10. 在 [屬性] 窗格中，將 [ **StaticDisplayLevels** ] 屬性設定為2。 Menu 控制項現在應該會在設計工具中顯示 [首頁]、[產品] 和 [服務] 節點。
11. 流覽瀏覽器中的頁面，以使用功能表。 （因為您之前新增至網站地圖的頁面實際上並不存在，當您嘗試並流覽至時，您會看到錯誤）。

試驗變更 StaticDisplayLevels 和 MaximumDynamicDisplayLevels 屬性，並查看它們如何影響功能表的呈現方式。

## <a name="lab-2-dynamically-binding-a-treeview-control"></a>實驗室2：動態繫結 TreeView 控制項

此練習假設您有 SQL Server 在本機執行，而且 Northwind 資料庫存在於 SQL Server 實例上。 如果不符合這些條件，請變更範例中的連接字串。 請注意，您可能也需要指定 SQL Server authentication，而不是信任的連接。

1. 建立新網站。
2. 切換至 default.aspx 的 [程式碼]，並以下列程式碼取代所有程式碼。 

    [!code-aspx[Main](data-bound-controls/samples/sample13.aspx)]
3. 將頁面儲存為 treeview .aspx。
4. 流覽頁面。
5. 第一次顯示網頁時，請在瀏覽器中查看頁面的來源。 請注意，只有可見的節點會傳送至用戶端。
6. 按一下任何節點旁邊的加號。
7. 再次查看頁面上的來源。 請注意，新顯示的節點現在已經存在。

## <a name="lab-3-details-view-and-editing-data-using-a-gridview-and-detailsview"></a>實驗室3：使用 GridView 和 DetailsView 詳細說明和編輯資料

1. 建立新網站。
2. 將新的 web.config 加入至網站。
3. 將連接字串新增至 web.config 檔案，如下所示： 

    [!code-xml[Main](data-bound-controls/samples/sample14.xml)]

    > [!NOTE]
    > 您可能需要根據您的環境變更連接字串。
4. 儲存並關閉 web.config 檔案。
5. 開啟 default.aspx 並加入新的 SqlDataSource 控制項。
6. 將 SqlDataSource 控制項的識別碼變更為**Products**。
7. 在 [ **SqlDataSource**工作] 功能表中，按一下 [**設定資料來源**]。
8. 在 [連接] 下拉式清單中選取**Northwind** ，然後按 [下一步]
9. 從 [**名稱**] 下拉式清單中選取 [**產品**]，然後檢查 [ **ProductID**， **ProductName**，**單價**] 和 [量表]**核取方塊，** 如下所示 

![](data-bound-controls/_static/image3.jpg)

    **Figure 3**
10. 按 [下一步]。
11. 按一下 [完成] **Walkthrough: Calling Code in an VSTO Add-in from VBA**。
12. 切換至來源視圖，並檢查所產生的程式碼。 請注意已新增至 SqlDataSource 控制項的**SelectCommand**、 **DeleteCommand**、 **InsertCommand**和**UpdateCommand** 。 另請注意已新增的參數。
13. 切換至設計檢視，並將新的 GridView 控制項加入至頁面。
14. 從 [**選擇資料來源**] 下拉式清單中選取 [**產品**]。
15. 核取 [**啟用分頁**] 和 [**啟用選取範圍**]，如下所示。 

![](data-bound-controls/_static/image4.jpg)

    **Figure 4**
16. 按一下 [**編輯資料行**] 連結，並確認已核取 [**自動產生欄位**]。
17. 按一下 [確定]。
18. 選取 GridView 控制項後，按一下 [屬性] 窗格中 [ **DataKeyNames** ] 屬性旁邊的按鈕。
19. 從 [可用的**資料欄位**] 清單中選取 [ **ProductID** ]，然後按一下 [ **&gt;** ] 按鈕將其加入。
20. 按一下 [確定]。
21. 將新的 SqlDataSource 控制項加入至頁面。
22. 將 SqlDataSource 控制項的識別碼變更為 [**詳細資料**]。
23. 在 [SqlDataSource 工作] 功能表中，選擇 [**設定資料來源**]。
24. 從下拉式清單中選擇 [ **Northwind** ]，然後按 **[下一步]** 。
25. 從 <strong>名稱</strong> 下拉式清單中選取 <strong>產品</strong>，然後勾選 資料<strong>行</strong> 清單方塊中的 <strong>\</strong >
26. 按一下 [ **WHERE** ] 按鈕。
27. 從 [資料**行**] 下拉式清單中選取 [ **ProductID** ]。
28. 在 [運算子] 下拉式清單中選取 [ **=** ]。
29. 從 [**來源**] 下拉式清單中選取 [**控制項**]。
30. 從 [**控制項識別碼**] 下拉式清單中選取 [ **GridView1** ]。
31. 按一下 [**新增**] 按鈕以加入 WHERE 子句。
32. 按一下 [確定]。
33. 按一下 [ **Advanced** ] 按鈕，然後選取 [**產生 INSERT、UPDATE 和 DELETE 子句**] 核取方塊。
34. 按一下 [確定]。
35. 按 **[下一步**] 並按一下 **[完成]** 。
36. 將 DetailsView 控制項新增至頁面。
37. 在 [**選擇資料來源**] 下拉式清單中，選擇 [**詳細**資料]。
38. 勾選 [**啟用編輯**] 核取方塊，如下所示。 

![](data-bound-controls/_static/image1.gif)

    **Figure 5**
39. 儲存頁面並流覽 default.aspx。
40. 按一下 [不同記錄] 旁的 [**選取**] 連結，以自動看到 DetailsView 更新。
41. 按一下 [DetailsView] 控制項中的 [**編輯**] 連結。
42. 對記錄進行變更，然後按一下 [**更新**]。
