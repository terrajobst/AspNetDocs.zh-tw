---
uid: mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
title: 在 ASP.NET MVC 4 中使用非同步方法 |Microsoft Docs
author: Rick-Anderson
description: 本教學課程將告訴您如何使用適用于 Web 的 Visual Studio Express 2012 建立異步 ASP.NET MVC Web 應用程式的基本概念，這是免費的 。
ms.author: riande
ms.date: 06/06/2012
ms.assetid: a56572ba-81c3-47af-826d-941e9c4775ec
msc.legacyurl: /mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
msc.type: authoredcontent
ms.openlocfilehash: 15692b18fc112c4c6cce4d50a243a0e8d5fb52a4
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 03/06/2020
ms.locfileid: "78538518"
---
# <a name="using-asynchronous-methods-in-aspnet-mvc-4"></a><span data-ttu-id="a4278-103">使用 ASP.NET MVC 4 中的非同步方法</span><span class="sxs-lookup"><span data-stu-id="a4278-103">Using Asynchronous Methods in ASP.NET MVC 4</span></span>

<span data-ttu-id="a4278-104">依[Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="a4278-104">by [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

> <span data-ttu-id="a4278-105">本教學課程將教您使用[Visual Studio Express 2012 For web](https://www.microsoft.com/visualstudio/11)（這是免費版本的 Microsoft Visual Studio）建立異步 ASP.NET MVC Web 應用程式的基本概念。</span><span class="sxs-lookup"><span data-stu-id="a4278-105">This tutorial will teach you the basics of building an asynchronous ASP.NET MVC Web application using [Visual Studio Express 2012 for Web](https://www.microsoft.com/visualstudio/11), which is a free version of Microsoft Visual Studio.</span></span> <span data-ttu-id="a4278-106">您也可以使用[Visual Studio 2012](https://www.microsoft.com/visualstudio/11)。</span><span class="sxs-lookup"><span data-stu-id="a4278-106">You can also use [Visual Studio 2012](https://www.microsoft.com/visualstudio/11).</span></span>
> 
> <span data-ttu-id="a4278-107">本教學課程的 github 上提供完整的範例[https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span><span class="sxs-lookup"><span data-stu-id="a4278-107">A complete sample is provided for this tutorial on github [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span></span>

<span data-ttu-id="a4278-108">結合[.net 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx)的 ASP.NET MVC 4[控制器](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx)類別，可讓您撰寫非同步動作方法，以傳回 Task 類型的物件， [&lt;ActionResult&gt;](https://msdn.microsoft.com/library/dd321424(VS.110).aspx)。</span><span class="sxs-lookup"><span data-stu-id="a4278-108">The ASP.NET MVC 4 [Controller](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx) class in combination [.NET 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx) enables you to write asynchronous action methods that return an object of type [Task&lt;ActionResult&gt;](https://msdn.microsoft.com/library/dd321424(VS.110).aspx).</span></span> <span data-ttu-id="a4278-109">.NET Framework 4 引進了[一種非同步](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)程式設計概念，稱為工作，而 ASP.NET MVC 4[支援工作](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)。</span><span class="sxs-lookup"><span data-stu-id="a4278-109">The .NET Framework 4 introduced an asynchronous programming concept referred to as a [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) and ASP.NET MVC 4 supports [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx).</span></span> <span data-ttu-id="a4278-110">**工作是以工作類型和**[[系統執行緒](https://msdn.microsoft.com/library/system.threading.tasks.aspx)] 命名空間中的相關類型來表示。</span><span class="sxs-lookup"><span data-stu-id="a4278-110">Tasks are represented by the **Task** type and related types in the [System.Threading.Tasks](https://msdn.microsoft.com/library/system.threading.tasks.aspx) namespace.</span></span> <span data-ttu-id="a4278-111">.NET Framework 4.5 是以具有[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)和[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)關鍵字的非同步支援為基礎，這些都是[使用工作物件，](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)而不是比先前的非同步方法更複雜。</span><span class="sxs-lookup"><span data-stu-id="a4278-111">The .NET Framework 4.5 builds on this asynchronous support with the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords that make working with [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) objects much less complex than previous asynchronous approaches.</span></span> <span data-ttu-id="a4278-112">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)關鍵字是語法速記，表示程式碼段應該以非同步方式等候某個程式碼的其他部分。</span><span class="sxs-lookup"><span data-stu-id="a4278-112">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword is syntactical shorthand for indicating that a piece of code should asynchronously wait on some other piece of code.</span></span> <span data-ttu-id="a4278-113">[Async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)關鍵字代表一個提示，可讓您將方法標示為以工作為基礎的非同步方法。</span><span class="sxs-lookup"><span data-stu-id="a4278-113">The [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword represents a hint that you can use to mark methods as task-based asynchronous methods.</span></span> <span data-ttu-id="a4278-114">**Await**、 **async**和**Task**物件的組合，可讓您更輕鬆地在 .net 4.5 中撰寫非同步程式碼。</span><span class="sxs-lookup"><span data-stu-id="a4278-114">The combination of **await**, **async**, and the **Task** object makes it much easier for you to write asynchronous code in .NET 4.5.</span></span> <span data-ttu-id="a4278-115">非同步方法的新模型稱為以工作為*基礎的非同步模式* **（點**一下）。</span><span class="sxs-lookup"><span data-stu-id="a4278-115">The new model for asynchronous methods is called the *Task-based Asynchronous Pattern* (**TAP**).</span></span> <span data-ttu-id="a4278-116">本教學課程假設您已熟悉使用[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)和[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)關鍵字[和工作命名](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)空間的非同步程式設計。</span><span class="sxs-lookup"><span data-stu-id="a4278-116">This tutorial assumes you have some familiarity with asynchronous programing using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace.</span></span>

<span data-ttu-id="a4278-117">如需有關使用[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)和[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)關鍵字和[工作命名空間](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)的詳細資訊，請參閱下列參考。</span><span class="sxs-lookup"><span data-stu-id="a4278-117">For more information on the using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the following references.</span></span>

- [<span data-ttu-id="a4278-118">技術白皮書： .NET 中的非同步</span><span class="sxs-lookup"><span data-stu-id="a4278-118">Whitepaper: Asynchrony in .NET</span></span>](https://go.microsoft.com/fwlink/?LinkId=204844)
- [<span data-ttu-id="a4278-119">Async/Await 常見問題</span><span class="sxs-lookup"><span data-stu-id="a4278-119">Async/Await FAQ</span></span>](https://blogs.msdn.com/b/pfxteam/archive/2012/04/12/10293335.aspx)
- [<span data-ttu-id="a4278-120">Visual Studio 非同步程式設計</span><span class="sxs-lookup"><span data-stu-id="a4278-120">Visual Studio Asynchronous Programming</span></span>](https://msdn.microsoft.com/vstudio/gg316360)

## <a id="HowRequestsProcessedByTP"></a><span data-ttu-id="a4278-121">執行緒集區處理要求的方式</span><span class="sxs-lookup"><span data-stu-id="a4278-121">How Requests Are Processed by the Thread Pool</span></span>

<span data-ttu-id="a4278-122">在 web 伺服器上，.NET Framework 會維護用來為 ASP.NET 要求提供服務的執行緒集區。</span><span class="sxs-lookup"><span data-stu-id="a4278-122">On the web server, the .NET Framework maintains a pool of threads that are used to service ASP.NET requests.</span></span> <span data-ttu-id="a4278-123">要求到達網頁伺服器時，集區中會分派一個執行緒來處理該要求。</span><span class="sxs-lookup"><span data-stu-id="a4278-123">When a request arrives, a thread from the pool is dispatched to process that request.</span></span> <span data-ttu-id="a4278-124">如果要求是以同步方式處理，處理要求的執行緒就會在處理要求時忙碌，而且該執行緒無法服務另一個要求。</span><span class="sxs-lookup"><span data-stu-id="a4278-124">If the request is processed synchronously, the thread that processes the request is busy while the request is being processed, and that thread cannot service another request.</span></span>   
  
<span data-ttu-id="a4278-125">這可能不是問題，因為執行緒集區的大小可以夠大，足以容納許多忙碌中的執行緒。</span><span class="sxs-lookup"><span data-stu-id="a4278-125">This might not be a problem, because the thread pool can be made large enough to accommodate many busy threads.</span></span> <span data-ttu-id="a4278-126">不過，執行緒集區中的執行緒數目有限（.NET 4.5 的預設最大值為5000）。</span><span class="sxs-lookup"><span data-stu-id="a4278-126">However, the number of threads in the thread pool is limited (the default maximum for .NET 4.5 is 5,000).</span></span> <span data-ttu-id="a4278-127">在高並行處理長時間執行之要求的大型應用程式中，所有可用的執行緒可能都很忙碌。</span><span class="sxs-lookup"><span data-stu-id="a4278-127">In large applications with high concurrency of long-running requests, all available threads might be busy.</span></span> <span data-ttu-id="a4278-128">這種狀況稱為「執行緒耗盡」(Thread Starvation)。</span><span class="sxs-lookup"><span data-stu-id="a4278-128">This condition is known as thread starvation.</span></span> <span data-ttu-id="a4278-129">當達到此條件時，web 伺服器會將要求排入佇列。</span><span class="sxs-lookup"><span data-stu-id="a4278-129">When this condition is reached, the web server queues requests.</span></span> <span data-ttu-id="a4278-130">如果要求佇列已滿，web 伺服器會拒絕 HTTP 503 狀態的要求（伺服器太忙碌）。</span><span class="sxs-lookup"><span data-stu-id="a4278-130">If the request queue becomes full, the web server rejects requests with an HTTP 503 status (Server Too Busy).</span></span> <span data-ttu-id="a4278-131">CLR 執行緒集區對於新的執行緒插入有限制。</span><span class="sxs-lookup"><span data-stu-id="a4278-131">The CLR thread pool has limitations on new thread injections.</span></span> <span data-ttu-id="a4278-132">如果並行處理是暴增的（也就是說，您的網站可能突然收到大量的要求），而且所有可用的要求執行緒都忙碌，因為具有高延遲的後端呼叫，限制的執行緒插入速率可能會讓您的應用程式回應變得非常差。</span><span class="sxs-lookup"><span data-stu-id="a4278-132">If concurrency is bursty (that is, your web site can suddenly get a large number of requests) and all available request threads are busy because of backend calls with high latency, the limited thread injection rate can make your application respond very poorly.</span></span> <span data-ttu-id="a4278-133">此外，新增至執行緒集區的每個新執行緒都有額外負荷（例如 1 MB 的堆疊記憶體）。</span><span class="sxs-lookup"><span data-stu-id="a4278-133">Additionally, each new thread added to the thread pool has overhead (such as 1 MB of stack memory).</span></span> <span data-ttu-id="a4278-134">使用同步方法來服務高延遲呼叫的 web 應用程式，其中線程集區會成長至 .NET 4.5 預設最大值5，000個執行緒會耗用大約 5 GB 的記憶體，而不是使用可服務相同要求的應用程式非同步方法和僅限50執行緒。</span><span class="sxs-lookup"><span data-stu-id="a4278-134">A web application using synchronous methods to service high latency calls where the thread pool grows to the .NET 4.5 default maximum of 5, 000 threads would consume approximately 5 GB more memory than an application able the service the same requests using asynchronous methods and only 50 threads.</span></span> <span data-ttu-id="a4278-135">當您執行非同步工作時，不一定是使用執行緒。</span><span class="sxs-lookup"><span data-stu-id="a4278-135">When you're doing asynchronous work, you're not always using a thread.</span></span> <span data-ttu-id="a4278-136">例如，當您進行非同步 web 服務要求時，ASP.NET 將不會在**非同步**方法呼叫和**await**之間使用任何執行緒。</span><span class="sxs-lookup"><span data-stu-id="a4278-136">For example, when you make an asynchronous web service request, ASP.NET will not be using any threads between the **async** method call and the **await**.</span></span> <span data-ttu-id="a4278-137">使用執行緒集區來服務具有高延遲的要求，可能會導致大量的記憶體耗用量和伺服器硬體的使用率不佳。</span><span class="sxs-lookup"><span data-stu-id="a4278-137">Using the thread pool to service requests with high latency can lead to a large memory footprint and poor utilization of the server hardware.</span></span>

## <a name="processing-asynchronous-requests"></a><span data-ttu-id="a4278-138">處理非同步要求</span><span class="sxs-lookup"><span data-stu-id="a4278-138">Processing Asynchronous Requests</span></span>

<span data-ttu-id="a4278-139">在啟動時看到大量並行要求或具有暴增負載的 web 應用程式（並行成長突然增加），讓 web 服務呼叫非同步增加應用程式的回應能力。</span><span class="sxs-lookup"><span data-stu-id="a4278-139">In a web app that sees a large number of concurrent requests at start-up or has a bursty load (where concurrency increases suddenly), making web service calls asynchronous increases the responsiveness of the app.</span></span> <span data-ttu-id="a4278-140">非同步要求所需的處理時間與同步要求相同。</span><span class="sxs-lookup"><span data-stu-id="a4278-140">An asynchronous request takes the same amount of time to process as a synchronous request.</span></span> <span data-ttu-id="a4278-141">如果要求發出 web 服務呼叫，需要兩秒的時間才能完成，則要求需要兩秒鐘的時間，不論是以同步或非同步方式執行。</span><span class="sxs-lookup"><span data-stu-id="a4278-141">If a request makes a web service call that requires two seconds to complete, the request takes two seconds whether it's performed synchronously or asynchronously.</span></span> <span data-ttu-id="a4278-142">不過，在非同步呼叫期間，執行緒在等候第一個要求完成時，不會遭到封鎖而無法回應其他要求。</span><span class="sxs-lookup"><span data-stu-id="a4278-142">However during an asynchronous call, a thread isn't blocked from responding to other requests while it waits for the first request to complete.</span></span> <span data-ttu-id="a4278-143">因此，當有多個並行要求會叫用長時間執行的作業時，非同步要求會阻止要求佇列和執行緒集區成長。</span><span class="sxs-lookup"><span data-stu-id="a4278-143">Therefore, asynchronous requests prevent request queuing and thread pool growth when there are many concurrent requests that invoke long-running operations.</span></span>

## <a id="ChoosingSyncVasync"></a><span data-ttu-id="a4278-144">選擇同步或非同步動作方法</span><span class="sxs-lookup"><span data-stu-id="a4278-144">Choosing Synchronous or Asynchronous Action Methods</span></span>

<span data-ttu-id="a4278-145">本節列出何時使用同步或非同步動作方法的方針。</span><span class="sxs-lookup"><span data-stu-id="a4278-145">This section lists guidelines for when to use synchronous or asynchronous action methods.</span></span> <span data-ttu-id="a4278-146">這些只是指導方針;個別檢查每個應用程式，以判斷非同步方法是否有助於效能。</span><span class="sxs-lookup"><span data-stu-id="a4278-146">These are just guidelines; examine each application individually to determine whether asynchronous methods help with performance.</span></span>

<span data-ttu-id="a4278-147">一般而言，在下列情況下，請使用同步方法：</span><span class="sxs-lookup"><span data-stu-id="a4278-147">In general, use synchronous methods for the following conditions:</span></span>

- <span data-ttu-id="a4278-148">作業很簡單或執行時間短暫。</span><span class="sxs-lookup"><span data-stu-id="a4278-148">The operations are simple or short-running.</span></span>
- <span data-ttu-id="a4278-149">簡潔比效率重要。</span><span class="sxs-lookup"><span data-stu-id="a4278-149">Simplicity is more important than efficiency.</span></span>
- <span data-ttu-id="a4278-150">作業主要都是 CPU 作業，而非需要耗用大量磁碟或網路的作業。</span><span class="sxs-lookup"><span data-stu-id="a4278-150">The operations are primarily CPU operations instead of operations that involve extensive disk or network overhead.</span></span> <span data-ttu-id="a4278-151">對受限於 CPU 的作業採用非同步動作並沒有好處，甚至會增加負擔。</span><span class="sxs-lookup"><span data-stu-id="a4278-151">Using asynchronous action methods on CPU-bound operations provides no benefits and results in more overhead.</span></span>

<span data-ttu-id="a4278-152">一般而言，在下列情況下，請使用非同步方法：</span><span class="sxs-lookup"><span data-stu-id="a4278-152">In general, use asynchronous methods for the following conditions:</span></span>

- <span data-ttu-id="a4278-153">您正在呼叫可透過非同步方法取用的服務，而且您使用的是 .NET 4.5 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="a4278-153">You're calling services that can be consumed through asynchronous methods, and you're using .NET 4.5 or higher.</span></span>
- <span data-ttu-id="a4278-154">作業受限於網路或 I/O，而非 CPU。</span><span class="sxs-lookup"><span data-stu-id="a4278-154">The operations are network-bound or I/O-bound instead of CPU-bound.</span></span>
- <span data-ttu-id="a4278-155">平行處理比簡化程式碼重要。</span><span class="sxs-lookup"><span data-stu-id="a4278-155">Parallelism is more important than simplicity of code.</span></span>
- <span data-ttu-id="a4278-156">您想提供可讓使用者取消長期執行要求的機制。</span><span class="sxs-lookup"><span data-stu-id="a4278-156">You want to provide a mechanism that lets users cancel a long-running request.</span></span>
- <span data-ttu-id="a4278-157">當切換執行緒的好處高於內容切換的成本時。</span><span class="sxs-lookup"><span data-stu-id="a4278-157">When the benefit of switching threads outweighs the cost of the context switch.</span></span> <span data-ttu-id="a4278-158">一般來說，如果同步方法在不執行任何工作的情況下等候 ASP.NET 要求執行緒，您應該將方法設為非同步。</span><span class="sxs-lookup"><span data-stu-id="a4278-158">In general, you should make a method asynchronous if the synchronous method waits on the ASP.NET request thread while doing no work.</span></span> <span data-ttu-id="a4278-159">藉由讓呼叫非同步，ASP.NET 要求執行緒在等待 web 服務要求完成時，不會停止執行任何工作。</span><span class="sxs-lookup"><span data-stu-id="a4278-159">By making the call asynchronous, the ASP.NET request thread is not stalled doing no work while it waits for the web service request to complete.</span></span>
- <span data-ttu-id="a4278-160">測試會顯示封鎖作業是網站效能的瓶頸，而 IIS 可以針對這些封鎖呼叫使用非同步方法來服務更多要求。</span><span class="sxs-lookup"><span data-stu-id="a4278-160">Testing shows that the blocking operations are a bottleneck in site performance and that IIS can service more requests by using asynchronous methods for these blocking calls.</span></span>

<span data-ttu-id="a4278-161">前述可下載的範例會說明如何有效地使用非同步動作方法。</span><span class="sxs-lookup"><span data-stu-id="a4278-161">The downloadable sample shows how to use asynchronous action methods effectively.</span></span> <span data-ttu-id="a4278-162">所提供的範例是設計用來在使用 .NET 4.5 的 ASP.NET MVC 4 中提供非同步程式設計的簡單示範。</span><span class="sxs-lookup"><span data-stu-id="a4278-162">The sample provided was designed to provide a simple demonstration of asynchronous programming in ASP.NET MVC 4 using .NET 4.5.</span></span> <span data-ttu-id="a4278-163">此範例不是為了在 ASP.NET MVC 中進行非同步程式設計的參考架構。</span><span class="sxs-lookup"><span data-stu-id="a4278-163">The sample is not intended to be a reference architecture for asynchronous programming in ASP.NET MVC.</span></span> <span data-ttu-id="a4278-164">範例程式會呼叫[ASP.NET Web API](../../../web-api/index.md)方法，然後再呼叫工作[。延遲](https://msdn.microsoft.com/library/hh139096(VS.110).aspx)以模擬長時間執行的 Web 服務呼叫。</span><span class="sxs-lookup"><span data-stu-id="a4278-164">The sample program calls [ASP.NET Web API](../../../web-api/index.md) methods which in turn call [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate long-running web service calls.</span></span> <span data-ttu-id="a4278-165">大部分的生產應用程式都不會顯示使用非同步動作方法的明顯好處。</span><span class="sxs-lookup"><span data-stu-id="a4278-165">Most production applications will not show such obvious benefits to using asynchronous action methods.</span></span>   
  
<span data-ttu-id="a4278-166">有些應用程式會要求所有動作方法都是非同步的。</span><span class="sxs-lookup"><span data-stu-id="a4278-166">Few applications require all action methods to be asynchronous.</span></span> <span data-ttu-id="a4278-167">有時候將一些同步動作方法轉換為非同步方法，對於所需的工作量而言可以提高最多效率。</span><span class="sxs-lookup"><span data-stu-id="a4278-167">Often, converting a few synchronous action methods to asynchronous methods provides the best efficiency increase for the amount of work required.</span></span>

## <a id="SampleApp"></a><span data-ttu-id="a4278-168">範例應用程式</span><span class="sxs-lookup"><span data-stu-id="a4278-168">The Sample Application</span></span>

<span data-ttu-id="a4278-169">您可以從[GitHub](https://github.com/)網站上的[https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET)下載範例應用程式。</span><span class="sxs-lookup"><span data-stu-id="a4278-169">You can download the sample application from [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET) on the [GitHub](https://github.com/) site.</span></span> <span data-ttu-id="a4278-170">存放庫是由三個專案所組成：</span><span class="sxs-lookup"><span data-stu-id="a4278-170">The repository consists of three projects:</span></span>

- <span data-ttu-id="a4278-171">*Mvc4Async*： ASP.NET MVC 4 專案，其中包含本教學課程中使用的程式碼。</span><span class="sxs-lookup"><span data-stu-id="a4278-171">*Mvc4Async*: The ASP.NET MVC 4 project that contains the code used in this tutorial.</span></span> <span data-ttu-id="a4278-172">它會對**WebAPIpgw**服務發出 Web API 呼叫。</span><span class="sxs-lookup"><span data-stu-id="a4278-172">It makes Web API calls to the **WebAPIpgw** service.</span></span>
- <span data-ttu-id="a4278-173">*WebAPIpgw*：用來執行 `Products, Gizmos and Widgets` 控制器的 ASP.NET MVC 4 Web API 專案。</span><span class="sxs-lookup"><span data-stu-id="a4278-173">*WebAPIpgw*: The ASP.NET MVC 4 Web API project that implements the `Products, Gizmos and Widgets` controllers.</span></span> <span data-ttu-id="a4278-174">它會提供*WebAppAsync*專案和*Mvc4Async*專案的資料。</span><span class="sxs-lookup"><span data-stu-id="a4278-174">It provides the data for the *WebAppAsync* project and the *Mvc4Async* project.</span></span>
- <span data-ttu-id="a4278-175">*WebAppAsync*：另一個教學課程中使用的 ASP.NET Web Forms 專案。</span><span class="sxs-lookup"><span data-stu-id="a4278-175">*WebAppAsync*: The ASP.NET Web Forms project used in another tutorial.</span></span>

## <a id="GizmosSynch"></a><span data-ttu-id="a4278-176">Gizmos 同步動作方法</span><span class="sxs-lookup"><span data-stu-id="a4278-176">The Gizmos Synchronous Action Method</span></span>

 <span data-ttu-id="a4278-177">下列程式碼顯示用來顯示 gizmos 清單的 `Gizmos` 同步動作方法。</span><span class="sxs-lookup"><span data-stu-id="a4278-177">The following code shows the `Gizmos` synchronous action method that is used to display a list of gizmos.</span></span> <span data-ttu-id="a4278-178">（在本文中，gizmo 是虛構的機械裝置）。</span><span class="sxs-lookup"><span data-stu-id="a4278-178">(For this article, a gizmo is a fictional mechanical device.)</span></span> 

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample1.cs)]

<span data-ttu-id="a4278-179">下列程式碼顯示 gizmo 服務的 `GetGizmos` 方法。</span><span class="sxs-lookup"><span data-stu-id="a4278-179">The following code shows the `GetGizmos` method of the gizmo service.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample2.cs)]

<span data-ttu-id="a4278-180">`GizmoService GetGizmos` 方法會將 URI 傳遞給 ASP.NET Web API HTTP 服務，以傳回 gizmos 資料的清單。</span><span class="sxs-lookup"><span data-stu-id="a4278-180">The `GizmoService GetGizmos` method passes a URI to an ASP.NET Web API HTTP service which returns a list of gizmos data.</span></span> <span data-ttu-id="a4278-181">*WebAPIpgw*專案包含 `gizmos, widget` 和 `product` 控制器的 Web API 的執行。</span><span class="sxs-lookup"><span data-stu-id="a4278-181">The *WebAPIpgw* project contains the implementation of the Web API `gizmos, widget` and `product` controllers.</span></span>  
<span data-ttu-id="a4278-182">下圖顯示範例專案中的 gizmos 視圖。</span><span class="sxs-lookup"><span data-stu-id="a4278-182">The following image shows the gizmos view from the sample project.</span></span>

![Gizmos](using-asynchronous-methods-in-aspnet-mvc-4/_static/image1.png)

## <a id="CreatingAsynchGizmos"></a><span data-ttu-id="a4278-184">建立異步 Gizmos 動作方法</span><span class="sxs-lookup"><span data-stu-id="a4278-184">Creating an Asynchronous Gizmos Action Method</span></span>

<span data-ttu-id="a4278-185">此範例使用新的[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)和[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)關鍵字（可在 .net 4.5 和 Visual Studio 2012 中取得），讓編譯器負責維護非同步程式設計所需的複雜轉換。</span><span class="sxs-lookup"><span data-stu-id="a4278-185">The sample uses the new [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) and [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keywords (available in .NET 4.5 and Visual Studio 2012) to let the compiler be responsible for maintaining the complicated transformations necessary for asynchronous programming.</span></span> <span data-ttu-id="a4278-186">編譯器可讓您使用C#的同步控制流程結構來撰寫程式碼，而編譯器會自動套用使用回呼所需的轉換，以避免封鎖執行緒。</span><span class="sxs-lookup"><span data-stu-id="a4278-186">The compiler lets you write code using the C#'s synchronous control flow constructs and the compiler automatically applies the transformations necessary to use callbacks in order to avoid blocking threads.</span></span>

<span data-ttu-id="a4278-187">下列程式碼顯示 `Gizmos` 同步方法和 `GizmosAsync` 非同步方法。</span><span class="sxs-lookup"><span data-stu-id="a4278-187">The following code shows the `Gizmos` synchronous method and the `GizmosAsync` asynchronous method.</span></span> <span data-ttu-id="a4278-188">如果您的瀏覽器支援[HTML 5 `<mark>` 元素](http://www.w3.org/wiki/HTML/Elements/mark)，您會在 `GizmosAsync` 的黃色醒目提示中看到變更。</span><span class="sxs-lookup"><span data-stu-id="a4278-188">If your browser supports the [HTML 5 `<mark>` element](http://www.w3.org/wiki/HTML/Elements/mark), you'll see the changes in `GizmosAsync` in yellow highlight.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample3.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample4.cs?highlight=1,3,5)]

 <span data-ttu-id="a4278-189">已套用下列變更，以允許 `GizmosAsync` 非同步。</span><span class="sxs-lookup"><span data-stu-id="a4278-189">The following changes were applied to allow the `GizmosAsync` to be asynchronous.</span></span>

- <span data-ttu-id="a4278-190">方法會以[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)關鍵字標示，這會告訴編譯器為本文的部分產生回呼，並自動建立傳回的 `Task<ActionResult>`。</span><span class="sxs-lookup"><span data-stu-id="a4278-190">The method is marked with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, which tells the compiler to generate callbacks for parts of the body and to automatically create a `Task<ActionResult>` that is returned.</span></span>
- <span data-ttu-id="a4278-191">&quot;非同步&quot; 附加至方法名稱。</span><span class="sxs-lookup"><span data-stu-id="a4278-191">&quot;Async&quot; was appended to the method name.</span></span> <span data-ttu-id="a4278-192">不需要附加 "Async"，而是撰寫非同步方法時的慣例。</span><span class="sxs-lookup"><span data-stu-id="a4278-192">Appending "Async" is not required but is the convention when writing asynchronous methods.</span></span>
- <span data-ttu-id="a4278-193">傳回類型已從 `ActionResult` 變更為 `Task<ActionResult>`。</span><span class="sxs-lookup"><span data-stu-id="a4278-193">The return type was changed from `ActionResult` to `Task<ActionResult>`.</span></span> <span data-ttu-id="a4278-194">`Task<ActionResult>` 的傳回類型代表進行中的工作，並提供方法的呼叫端，以及等候非同步作業完成的控制碼。</span><span class="sxs-lookup"><span data-stu-id="a4278-194">The return type of `Task<ActionResult>` represents ongoing work and provides callers of the method with a handle through which to wait for the asynchronous operation's completion.</span></span> <span data-ttu-id="a4278-195">在此情況下，呼叫端是 web 服務。</span><span class="sxs-lookup"><span data-stu-id="a4278-195">In this case, the caller is the web service.</span></span> <span data-ttu-id="a4278-196">`Task<ActionResult>` 代表進行中的工作，其結果為 `ActionResult.`</span><span class="sxs-lookup"><span data-stu-id="a4278-196">`Task<ActionResult>` represents ongoing work with a result of `ActionResult.`</span></span>
- <span data-ttu-id="a4278-197">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)關鍵字已套用至 web 服務呼叫。</span><span class="sxs-lookup"><span data-stu-id="a4278-197">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the web service call.</span></span>
- <span data-ttu-id="a4278-198">已呼叫非同步 web 服務 API （`GetGizmosAsync`）。</span><span class="sxs-lookup"><span data-stu-id="a4278-198">The asynchronous web service API was called (`GetGizmosAsync`).</span></span>

<span data-ttu-id="a4278-199">在 `GetGizmosAsync` 方法主體內，另一個非同步方法，`GetGizmosAsync` 呼叫。</span><span class="sxs-lookup"><span data-stu-id="a4278-199">Inside of the `GetGizmosAsync` method body another asynchronous method, `GetGizmosAsync` is called.</span></span> <span data-ttu-id="a4278-200">`GetGizmosAsync` 立即傳回當資料可供使用時，最後會完成的 `Task<List<Gizmo>>`。</span><span class="sxs-lookup"><span data-stu-id="a4278-200">`GetGizmosAsync` immediately returns a `Task<List<Gizmo>>` that will eventually complete when the data is available.</span></span> <span data-ttu-id="a4278-201">因為您不想要在擁有 gizmo 資料之前執行任何其他動作，所以程式碼會等候工作（使用**await**關鍵字）。</span><span class="sxs-lookup"><span data-stu-id="a4278-201">Because you don't want to do anything else until you have the gizmo data, the code awaits the task (using the **await** keyword).</span></span> <span data-ttu-id="a4278-202">您只能在以**async**關鍵字標注的方法中使用**await**關鍵字。</span><span class="sxs-lookup"><span data-stu-id="a4278-202">You can use the **await** keyword only in methods annotated with the **async** keyword.</span></span>

<span data-ttu-id="a4278-203">**Await**關鍵字不會封鎖執行緒，直到工作完成為止。</span><span class="sxs-lookup"><span data-stu-id="a4278-203">The **await** keyword does not block the thread until the task is complete.</span></span> <span data-ttu-id="a4278-204">它會將方法的其餘部分註冊為工作上的回呼，並立即傳回。</span><span class="sxs-lookup"><span data-stu-id="a4278-204">It signs up the rest of the method as a callback on the task, and immediately returns.</span></span> <span data-ttu-id="a4278-205">當等候的工作最後完成時，它會叫用該回呼，並繼續執行方法，並將其停止。</span><span class="sxs-lookup"><span data-stu-id="a4278-205">When the awaited task eventually completes, it will invoke that callback and thus resume the execution of the method right where it left off.</span></span> <span data-ttu-id="a4278-206">如需使用[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)和[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)關鍵字和[工作命名空間](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)的詳細資訊，請參閱[非同步參考](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async)。</span><span class="sxs-lookup"><span data-stu-id="a4278-206">For more information on using the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the [async references](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async).</span></span>

<span data-ttu-id="a4278-207">下列程式碼顯示 `GetGizmos` 和 `GetGizmosAsync` 方法。</span><span class="sxs-lookup"><span data-stu-id="a4278-207">The following code shows the `GetGizmos` and `GetGizmosAsync` methods.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample5.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample6.cs?highlight=1,4-8)]

 <span data-ttu-id="a4278-208">非同步變更與上述**GizmosAsync**所做的變更類似。</span><span class="sxs-lookup"><span data-stu-id="a4278-208">The asynchronous changes are similar to those made to the **GizmosAsync** above.</span></span> 

- <span data-ttu-id="a4278-209">方法簽章是以[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)關鍵字標注，傳回型別已變更為 `Task<List<Gizmo>>`，而*async*已附加至方法名稱。</span><span class="sxs-lookup"><span data-stu-id="a4278-209">The method signature was annotated with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, the return type was changed to `Task<List<Gizmo>>`, and *Async* was appended to the method name.</span></span>
- <span data-ttu-id="a4278-210">會使用非同步[HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx)類別，而不是[WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx)類別。</span><span class="sxs-lookup"><span data-stu-id="a4278-210">The asynchronous [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) class is used instead of the [WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx) class.</span></span>
- <span data-ttu-id="a4278-211">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)關鍵字已套用至[HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx)非同步方法。</span><span class="sxs-lookup"><span data-stu-id="a4278-211">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) asynchronous methods.</span></span>

<span data-ttu-id="a4278-212">下圖顯示 [非同步 gizmo] 視圖。</span><span class="sxs-lookup"><span data-stu-id="a4278-212">The following image shows the asynchronous gizmo view.</span></span>

![async](using-asynchronous-methods-in-aspnet-mvc-4/_static/image2.png)

<span data-ttu-id="a4278-214">Gizmos 資料的瀏覽器呈現方式與同步呼叫所建立的視圖相同。</span><span class="sxs-lookup"><span data-stu-id="a4278-214">The browsers presentation of the gizmos data is identical to the view created by the synchronous call.</span></span> <span data-ttu-id="a4278-215">唯一的差異在於，非同步版本在繁重的負載下可能會更有效率。</span><span class="sxs-lookup"><span data-stu-id="a4278-215">The only difference is the asynchronous version may be more performant under heavy loads.</span></span>

## <a id="Parallel"></a><span data-ttu-id="a4278-216">平行執行多個作業</span><span class="sxs-lookup"><span data-stu-id="a4278-216">Performing Multiple Operations in Parallel</span></span>

<span data-ttu-id="a4278-217">當動作必須執行數個獨立的作業時，非同步動作方法在同步方法上會有相當大的優勢。</span><span class="sxs-lookup"><span data-stu-id="a4278-217">Asynchronous action methods have a significant advantage over synchronous methods when an action must perform several independent operations.</span></span> <span data-ttu-id="a4278-218">在提供的範例中，同步方法 `PWG`（適用于產品、widget 和 Gizmos）會顯示三個 web 服務呼叫的結果，以取得產品、widget 和 Gizmos 的清單。</span><span class="sxs-lookup"><span data-stu-id="a4278-218">In the sample provided, the synchronous method `PWG`(for Products, Widgets and Gizmos) displays the results of three web service calls to get a list of products, widgets, and gizmos.</span></span> <span data-ttu-id="a4278-219">提供這些服務的[ASP.NET Web API](../../../web-api/index.md)專案會使用 [[延遲](https://msdn.microsoft.com/library/hh139096(VS.110).aspx)] 來模擬延遲或緩慢的網路呼叫。</span><span class="sxs-lookup"><span data-stu-id="a4278-219">The [ASP.NET Web API](../../../web-api/index.md) project that provides these services uses [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate latency or slow network calls.</span></span> <span data-ttu-id="a4278-220">當延遲時間設定為500毫秒時，非同步 `PWGasync` 方法會花上500毫秒來完成，而同步 `PWG` 版本則需要超過1500毫秒。</span><span class="sxs-lookup"><span data-stu-id="a4278-220">When the delay is set to 500 milliseconds, the asynchronous `PWGasync` method takes a little over 500 milliseconds to complete while the synchronous `PWG` version takes over 1,500 milliseconds.</span></span> <span data-ttu-id="a4278-221">同步 `PWG` 方法會顯示在下列程式碼中。</span><span class="sxs-lookup"><span data-stu-id="a4278-221">The synchronous `PWG` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample7.cs)]

<span data-ttu-id="a4278-222">非同步 `PWGasync` 方法會顯示在下列程式碼中。</span><span class="sxs-lookup"><span data-stu-id="a4278-222">The asynchronous `PWGasync` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample8.cs?highlight=1,3,12)]

<span data-ttu-id="a4278-223">下圖顯示從**PWGasync**方法傳回的視圖。</span><span class="sxs-lookup"><span data-stu-id="a4278-223">The following image shows the view returned from the **PWGasync** method.</span></span>

![pwgAsync](using-asynchronous-methods-in-aspnet-mvc-4/_static/image3.png)

## <a id="CancelToken"></a><span data-ttu-id="a4278-225">使用解除標記</span><span class="sxs-lookup"><span data-stu-id="a4278-225">Using a Cancellation Token</span></span>

<span data-ttu-id="a4278-226">傳回 `Task<ActionResult>`的非同步動作方法是可取消的，而當[AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx)屬性提供一個參數時，則會接受[CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx)參數。</span><span class="sxs-lookup"><span data-stu-id="a4278-226">Asynchronous action methods returning `Task<ActionResult>`are cancelable, that is they take a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter when one is provided with the [AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx) attribute.</span></span> <span data-ttu-id="a4278-227">下列程式碼顯示 `GizmosCancelAsync` 方法，其超時時間為150毫秒。</span><span class="sxs-lookup"><span data-stu-id="a4278-227">The following code shows the `GizmosCancelAsync` method with a timeout of 150 milliseconds.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample9.cs?highlight=1-3,5,10)]

<span data-ttu-id="a4278-228">下列程式碼顯示 GetGizmosAsync 多載，它採用[CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx)參數。</span><span class="sxs-lookup"><span data-stu-id="a4278-228">The following code shows the GetGizmosAsync overload, which takes a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample10.cs)]

<span data-ttu-id="a4278-229">在提供的範例應用程式中，選取 [*取消權杖示範*] 連結會呼叫 `GizmosCancelAsync` 方法，並示範取消非同步呼叫。</span><span class="sxs-lookup"><span data-stu-id="a4278-229">In the sample application provided, selecting the *Cancellation Token Demo* link calls the `GizmosCancelAsync` method and demonstrates the cancellation of the asynchronous call.</span></span>

## <a id="ServerConfig"></a><span data-ttu-id="a4278-230">高平行存取/高延遲 Web 服務呼叫的伺服器設定</span><span class="sxs-lookup"><span data-stu-id="a4278-230">Server Configuration for High Concurrency/High Latency Web Service Calls</span></span>

<span data-ttu-id="a4278-231">若要實現非同步 web 應用程式的優點，您可能需要對預設伺服器設定進行一些變更。</span><span class="sxs-lookup"><span data-stu-id="a4278-231">To realize the benefits of an asynchronous web application, you might need to make some changes to the default server configuration.</span></span> <span data-ttu-id="a4278-232">設定和壓力測試非同步 web 應用程式時，請記住下列事項。</span><span class="sxs-lookup"><span data-stu-id="a4278-232">Keep the following in mind when configuring and stress testing your asynchronous web application.</span></span>

- <span data-ttu-id="a4278-233">Windows 7、Windows Vista 及所有 Windows 用戶端作業系統最多可以有10個並行要求。</span><span class="sxs-lookup"><span data-stu-id="a4278-233">Windows 7, Windows Vista and all Windows client operating systems have a maximum of 10 concurrent requests.</span></span> <span data-ttu-id="a4278-234">您需要 Windows Server 作業系統，才能在高負載下查看非同步方法的優點。</span><span class="sxs-lookup"><span data-stu-id="a4278-234">You'll need a Windows Server operating system to see the benefits of asynchronous methods under high load.</span></span>
- <span data-ttu-id="a4278-235">從提升許可權的命令提示字元，向 IIS 註冊 .NET 4.5：</span><span class="sxs-lookup"><span data-stu-id="a4278-235">Register .NET 4.5 with IIS from an elevated command prompt:</span></span>  
  <span data-ttu-id="a4278-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis-i</span><span class="sxs-lookup"><span data-stu-id="a4278-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis -i</span></span>  
  <span data-ttu-id="a4278-237">請參閱[ASP.NET IIS 註冊工具（Aspnet\_regiis）](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span><span class="sxs-lookup"><span data-stu-id="a4278-237">See    [ASP.NET IIS Registration Tool (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span></span>
- <span data-ttu-id="a4278-238">您可能需要將[HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture)佇列限制從預設值1000增加至5000。</span><span class="sxs-lookup"><span data-stu-id="a4278-238">You might need to increase the [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) queue limit from the default value of 1,000 to 5,000.</span></span> <span data-ttu-id="a4278-239">如果設定太低，您可能會看到 HTTP.sys 拒絕要求，[其 HTTP 為](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture)503 狀態。</span><span class="sxs-lookup"><span data-stu-id="a4278-239">If the setting is too low, you may see [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) reject requests with a HTTP 503 status.</span></span> <span data-ttu-id="a4278-240">若要變更 HTTP.sys 佇列限制：</span><span class="sxs-lookup"><span data-stu-id="a4278-240">To change the HTTP.sys queue limit:</span></span>

    - <span data-ttu-id="a4278-241">開啟 [IIS 管理員]，然後流覽至 [應用程式集區] 窗格。</span><span class="sxs-lookup"><span data-stu-id="a4278-241">Open IIS manager and navigate to the Application Pools pane.</span></span>
    - <span data-ttu-id="a4278-242">以滑鼠右鍵按一下目標應用程式集區，然後選取 [ **Advanced Settings**]。</span><span class="sxs-lookup"><span data-stu-id="a4278-242">Right click on the target application pool and select **Advanced Settings**.</span></span>  
        <span data-ttu-id="a4278-243">![advanced](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="a4278-243">![advanced](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span></span>
    - <span data-ttu-id="a4278-244">在 [**高級設定**] 對話方塊中，將 [*佇列長度*] 從1000變更為5000。</span><span class="sxs-lookup"><span data-stu-id="a4278-244">In the **Advanced Settings** dialog box, change *Queue Length* from 1,000 to 5,000.</span></span>  
        <span data-ttu-id="a4278-245">![佇列長度](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span><span class="sxs-lookup"><span data-stu-id="a4278-245">![Queue length](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span></span>  
  
  <span data-ttu-id="a4278-246">請注意，在上述影像中，即使應用程式集區使用 .NET 4.5，.NET framework 仍會列為 v4.0。</span><span class="sxs-lookup"><span data-stu-id="a4278-246">Note in the images above, the .NET framework is listed as v4.0, even though the application pool is using .NET 4.5.</span></span> <span data-ttu-id="a4278-247">若要瞭解這項差異，請參閱下列各項：</span><span class="sxs-lookup"><span data-stu-id="a4278-247">To understand this discrepancy, see the following:</span></span>

    - [<span data-ttu-id="a4278-248">.NET 版本控制和多目標-.NET 4.5 是就地升級至 .NET 4。0</span><span class="sxs-lookup"><span data-stu-id="a4278-248">.NET Versioning and Multi-Targeting - .NET 4.5 is an in-place upgrade to .NET 4.0</span></span>](http://www.hanselman.com/blog/NETVersioningAndMultiTargetingNET45IsAnInplaceUpgradeToNET40.aspx)
    - [<span data-ttu-id="a4278-249">如何將 IIS 應用程式或 AppPool 設定為使用 ASP.NET 3.5，而不是2。0</span><span class="sxs-lookup"><span data-stu-id="a4278-249">How to set an IIS Application or AppPool to use ASP.NET 3.5 rather than 2.0</span></span>](http://www.hanselman.com/blog/HowToSetAnIISApplicationOrAppPoolToUseASPNET35RatherThan20.aspx)
    - <span data-ttu-id="a4278-250">[.NET Framework 版本和相依性](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span><span class="sxs-lookup"><span data-stu-id="a4278-250">[.NET Framework Versions and Dependencies](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span></span>
- <span data-ttu-id="a4278-251">如果您的應用程式使用 web 服務或 System.NET 透過 HTTP 與後端通訊，您可能需要增加[connectionmanagement 專案/maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx)元素。</span><span class="sxs-lookup"><span data-stu-id="a4278-251">If your application is using web services or System.NET to communicate with a backend over HTTP you may need to increase the [connectionManagement/maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx) element.</span></span> <span data-ttu-id="a4278-252">針對 ASP.NET 應用程式，這會受限於 Cpu 數目12倍的自動設定功能。</span><span class="sxs-lookup"><span data-stu-id="a4278-252">For ASP.NET applications, this is limited by the autoConfig feature to 12 times the number of CPUs.</span></span> <span data-ttu-id="a4278-253">這表示在四個處理器上，您最多可以有12個 \* 4 = 48 個對 IP 端點的並行連線。</span><span class="sxs-lookup"><span data-stu-id="a4278-253">That means that on a quad-proc, you can have at most 12 \* 4 = 48 concurrent connections to an IP end point.</span></span> <span data-ttu-id="a4278-254">因為這會系結[至自動](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx)設定，在 ASP.NET 應用程式中增加 `maxconnection` 最簡單的方式，就是在*global.asax*檔案的 from `Application_Start` 方法中，以程式設計方式設定[ServicePointManager servicepointmanager.defaultconnectionlimit。](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx)</span><span class="sxs-lookup"><span data-stu-id="a4278-254">Because this is tied to [autoConfig](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx), the easiest way to increase `maxconnection` in an ASP.NET application is to set [System.Net.ServicePointManager.DefaultConnectionLimit](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx) programmatically in the from `Application_Start` method in the *global.asax* file.</span></span> <span data-ttu-id="a4278-255">如需範例，請參閱範例下載。</span><span class="sxs-lookup"><span data-stu-id="a4278-255">See the sample download for an example.</span></span>
- <span data-ttu-id="a4278-256">在 .NET 4.5 中， [MaxConcurrentRequestsPerCPU](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx)的預設值應該是 [5000]。</span><span class="sxs-lookup"><span data-stu-id="a4278-256">In .NET 4.5, the default of 5000 for [MaxConcurrentRequestsPerCPU](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx) should be fine.</span></span>
