---
uid: mvc/overview/older-versions-1/contact-manager/iteration-7-add-ajax-functionality-vb
title: '反復專案 #7 –新增 Ajax 功能（VB） |Microsoft Docs'
author: microsoft
description: 在第七次的反復專案中，我們藉由新增 Ajax 的支援來改善應用程式的回應性和效能。
ms.author: riande
ms.date: 02/20/2009
ms.assetid: f640e063-150e-453d-8cfc-7e54a6ce0f1e
msc.legacyurl: /mvc/overview/older-versions-1/contact-manager/iteration-7-add-ajax-functionality-vb
msc.type: authoredcontent
ms.openlocfilehash: cee2b6e7c7517a1e03ae26d5233fc438857a030c
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 03/06/2020
ms.locfileid: "78601651"
---
# <a name="iteration-7--add-ajax-functionality-vb"></a><span data-ttu-id="bda78-103">反復專案 #7 –新增 Ajax 功能（VB）</span><span class="sxs-lookup"><span data-stu-id="bda78-103">Iteration #7 – Add Ajax functionality (VB)</span></span>

<span data-ttu-id="bda78-104">由[Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="bda78-104">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="bda78-105">下載程式代碼</span><span class="sxs-lookup"><span data-stu-id="bda78-105">Download Code</span></span>](iteration-7-add-ajax-functionality-vb/_static/contactmanager_7_vb1.zip)

> <span data-ttu-id="bda78-106">在第七次的反復專案中，我們藉由新增 Ajax 的支援來改善應用程式的回應性和效能。</span><span class="sxs-lookup"><span data-stu-id="bda78-106">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>

## <a name="building-a-contact-management-aspnet-mvc-application-vb"></a><span data-ttu-id="bda78-107">建立連絡人管理 ASP.NET MVC 應用程式（VB）</span><span class="sxs-lookup"><span data-stu-id="bda78-107">Building a Contact Management ASP.NET MVC Application (VB)</span></span>

<span data-ttu-id="bda78-108">在這一系列的教學課程中，我們從一開始就建立了整個連絡人管理應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-108">In this series of tutorials, we build an entire Contact Management application from start to finish.</span></span> <span data-ttu-id="bda78-109">連絡人管理員應用程式可讓您儲存連絡人資訊名稱、電話號碼和電子郵件地址，以取得人員清單。</span><span class="sxs-lookup"><span data-stu-id="bda78-109">The Contact Manager application enables you to store contact information - names, phone numbers and email addresses - for a list of people.</span></span>

<span data-ttu-id="bda78-110">我們會透過多個反復專案來建立應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-110">We build the application over multiple iterations.</span></span> <span data-ttu-id="bda78-111">在每次反覆運算時，我們會逐漸改善應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-111">With each iteration, we gradually improve the application.</span></span> <span data-ttu-id="bda78-112">這個多個反復專案方法的目標，是要讓您瞭解每項變更的原因。</span><span class="sxs-lookup"><span data-stu-id="bda78-112">The goal of this multiple iteration approach is to enable you to understand the reason for each change.</span></span>

- <span data-ttu-id="bda78-113">反復專案 #1-建立應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-113">Iteration #1 - Create the application.</span></span> <span data-ttu-id="bda78-114">在第一個反復專案中，我們會以最簡單的方式建立連絡人管理員。</span><span class="sxs-lookup"><span data-stu-id="bda78-114">In the first iteration, we create the Contact Manager in the simplest way possible.</span></span> <span data-ttu-id="bda78-115">我們新增對基本資料庫作業的支援：建立、讀取、更新和刪除（CRUD）。</span><span class="sxs-lookup"><span data-stu-id="bda78-115">We add support for basic database operations: Create, Read, Update, and Delete (CRUD).</span></span>

- <span data-ttu-id="bda78-116">反復專案 #2-讓應用程式看起來不錯。</span><span class="sxs-lookup"><span data-stu-id="bda78-116">Iteration #2 - Make the application look nice.</span></span> <span data-ttu-id="bda78-117">在此反復專案中，我們藉由修改預設的 ASP.NET MVC view 主版頁面和級聯樣式表，來改善應用程式的外觀。</span><span class="sxs-lookup"><span data-stu-id="bda78-117">In this iteration, we improve the appearance of the application by modifying the default ASP.NET MVC view master page and cascading style sheet.</span></span>

- <span data-ttu-id="bda78-118">反復專案 #3-新增表單驗證。</span><span class="sxs-lookup"><span data-stu-id="bda78-118">Iteration #3 - Add form validation.</span></span> <span data-ttu-id="bda78-119">在第三個反復專案中，我們會新增基本表單驗證。</span><span class="sxs-lookup"><span data-stu-id="bda78-119">In the third iteration, we add basic form validation.</span></span> <span data-ttu-id="bda78-120">我們會防止人們提交表單，而不需要完成必要的表單欄位。</span><span class="sxs-lookup"><span data-stu-id="bda78-120">We prevent people from submitting a form without completing required form fields.</span></span> <span data-ttu-id="bda78-121">我們也會驗證電子郵件地址和電話號碼。</span><span class="sxs-lookup"><span data-stu-id="bda78-121">We also validate email addresses and phone numbers.</span></span>

- <span data-ttu-id="bda78-122">反復專案 #4-讓應用程式鬆散結合。</span><span class="sxs-lookup"><span data-stu-id="bda78-122">Iteration #4 - Make the application loosely coupled.</span></span> <span data-ttu-id="bda78-123">在這第四次的反復專案中，我們會利用數種軟體設計模式，讓您更輕鬆地維護和修改 Contact Manager 應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-123">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="bda78-124">例如，我們會重構應用程式，以使用存放庫模式和相依性插入模式。</span><span class="sxs-lookup"><span data-stu-id="bda78-124">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

- <span data-ttu-id="bda78-125">反復專案 #5-建立單元測試。</span><span class="sxs-lookup"><span data-stu-id="bda78-125">Iteration #5 - Create unit tests.</span></span> <span data-ttu-id="bda78-126">在第五個反復專案中，我們會藉由新增單元測試，讓應用程式更容易維護和修改。</span><span class="sxs-lookup"><span data-stu-id="bda78-126">In the fifth iteration, we make our application easier to maintain and modify by adding unit tests.</span></span> <span data-ttu-id="bda78-127">我們會模擬我們的資料模型類別，並為我們的控制器和驗證邏輯建立單元測試。</span><span class="sxs-lookup"><span data-stu-id="bda78-127">We mock our data model classes and build unit tests for our controllers and validation logic.</span></span>

- <span data-ttu-id="bda78-128">反復專案 #6-使用以測試為導向的開發。</span><span class="sxs-lookup"><span data-stu-id="bda78-128">Iteration #6 - Use test-driven development.</span></span> <span data-ttu-id="bda78-129">在此第六個反復專案中，我們會先撰寫單元測試，並針對單元測試撰寫程式碼，以將新功能加入至應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-129">In this sixth iteration, we add new functionality to our application by writing unit tests first and writing code against the unit tests.</span></span> <span data-ttu-id="bda78-130">在此反復專案中，我們會新增連絡人群組。</span><span class="sxs-lookup"><span data-stu-id="bda78-130">In this iteration, we add contact groups.</span></span>

- <span data-ttu-id="bda78-131">反復專案 #7-新增 Ajax 功能。</span><span class="sxs-lookup"><span data-stu-id="bda78-131">Iteration #7 - Add Ajax functionality.</span></span> <span data-ttu-id="bda78-132">在第七次的反復專案中，我們藉由新增 Ajax 的支援來改善應用程式的回應性和效能。</span><span class="sxs-lookup"><span data-stu-id="bda78-132">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>

## <a name="this-iteration"></a><span data-ttu-id="bda78-133">這個反復專案</span><span class="sxs-lookup"><span data-stu-id="bda78-133">This Iteration</span></span>

<span data-ttu-id="bda78-134">在此「連絡人管理員」應用程式的反復專案中，我們會重構應用程式，以利用 Ajax。</span><span class="sxs-lookup"><span data-stu-id="bda78-134">In this iteration of the Contact Manager application, we refactor our application to make use of Ajax.</span></span> <span data-ttu-id="bda78-135">藉由利用 Ajax，我們讓應用程式更具回應性。</span><span class="sxs-lookup"><span data-stu-id="bda78-135">By taking advantage of Ajax, we make our application more responsive.</span></span> <span data-ttu-id="bda78-136">當我們需要只更新頁面中的特定區域時，我們可以避免轉譯整個頁面。</span><span class="sxs-lookup"><span data-stu-id="bda78-136">We can avoid rendering an entire page when we need to update only a certain region in a page.</span></span>

<span data-ttu-id="bda78-137">我們會重構索引視圖，如此一來，每當有人選取新的連絡人群組時，就不需要重新顯示整個頁面。</span><span class="sxs-lookup"><span data-stu-id="bda78-137">We'll refactor our Index view so that we don t need to redisplay the entire page whenever someone selects a new contact group.</span></span> <span data-ttu-id="bda78-138">相反地，當有人按一下連絡人群組時，我們只會更新連絡人清單，並保留其餘的網頁。</span><span class="sxs-lookup"><span data-stu-id="bda78-138">Instead, when someone clicks a contact group, we'll just update the list of contacts and leave the rest of the page alone.</span></span>

<span data-ttu-id="bda78-139">我們也會變更刪除連結的運作方式。</span><span class="sxs-lookup"><span data-stu-id="bda78-139">We'll also change the way our delete link works.</span></span> <span data-ttu-id="bda78-140">我們不會顯示個別的確認頁面，而是顯示 JavaScript 確認對話方塊。</span><span class="sxs-lookup"><span data-stu-id="bda78-140">Instead of displaying a separate confirmation page, we'll display a JavaScript confirmation dialog.</span></span> <span data-ttu-id="bda78-141">如果您確認要刪除連絡人，則會針對伺服器執行 HTTP 刪除作業，以從資料庫中刪除連絡人記錄。</span><span class="sxs-lookup"><span data-stu-id="bda78-141">If you confirm that you want to delete a contact, an HTTP DELETE operation is performed against the server to delete the contact record from the database.</span></span>

<span data-ttu-id="bda78-142">此外，我們將利用 jQuery 將動畫效果加入至索引視圖。</span><span class="sxs-lookup"><span data-stu-id="bda78-142">Furthermore, we will take advantage of jQuery to add animation effects to our Index view.</span></span> <span data-ttu-id="bda78-143">當從伺服器提取新的連絡人清單時，我們會顯示動畫。</span><span class="sxs-lookup"><span data-stu-id="bda78-143">We'll display an animation when the new list of contacts is being fetched from the server.</span></span>

<span data-ttu-id="bda78-144">最後，我們將利用 ASP.NET AJAX framework 支援來管理瀏覽器歷程記錄。</span><span class="sxs-lookup"><span data-stu-id="bda78-144">Finally, we'll take advantage of the ASP.NET AJAX framework support for managing browser history.</span></span> <span data-ttu-id="bda78-145">當我們執行 Ajax 呼叫來更新連絡人清單時，我們會建立歷程記錄點。</span><span class="sxs-lookup"><span data-stu-id="bda78-145">We'll create history points whenever we perform an Ajax call to update the contact list.</span></span> <span data-ttu-id="bda78-146">如此一來，瀏覽器的 [回溯] 和 [下一頁] 按鈕就會生效。</span><span class="sxs-lookup"><span data-stu-id="bda78-146">That way, the browser backward and forward buttons will work.</span></span>

## <a name="why-use-ajax"></a><span data-ttu-id="bda78-147">為何要使用 Ajax？</span><span class="sxs-lookup"><span data-stu-id="bda78-147">Why use Ajax?</span></span>

<span data-ttu-id="bda78-148">使用 Ajax 有許多優點。</span><span class="sxs-lookup"><span data-stu-id="bda78-148">Using Ajax has many benefits.</span></span> <span data-ttu-id="bda78-149">首先，將 Ajax 功能新增至應用程式會導致更好的使用者體驗。</span><span class="sxs-lookup"><span data-stu-id="bda78-149">First, adding Ajax functionality to an application results in a better user experience.</span></span> <span data-ttu-id="bda78-150">在一般 web 應用程式中，每次使用者執行動作時，整個頁面都必須回傳至伺服器。</span><span class="sxs-lookup"><span data-stu-id="bda78-150">In a normal web application, the entire page must be posted back to the server each and every time a user performs an action.</span></span> <span data-ttu-id="bda78-151">每當您執行動作時，瀏覽器就會鎖定，使用者必須等到整個頁面被提取並重新顯示為止。</span><span class="sxs-lookup"><span data-stu-id="bda78-151">Whenever you perform an action, the browser locks and the user must wait until the entire page is fetched and redisplayed.</span></span>

<span data-ttu-id="bda78-152">在桌面應用程式的情況下，這會是無法接受的體驗。</span><span class="sxs-lookup"><span data-stu-id="bda78-152">This would be an unacceptable experience in the case of a desktop application.</span></span> <span data-ttu-id="bda78-153">但是，在傳統上，我們在 web 應用程式的情況下會遇到這種不正確的使用者體驗，因為我們並不知道我們可以做得更好。</span><span class="sxs-lookup"><span data-stu-id="bda78-153">But, traditionally, we lived with this bad user experience in the case of a web application because we did not know that we could do any better.</span></span> <span data-ttu-id="bda78-154">我們認為這是 web 應用程式的限制，但實際上，這只是我們 imaginations 的限制。</span><span class="sxs-lookup"><span data-stu-id="bda78-154">We thought it was a limitation of web applications when, in actuality, it was just a limitation of our imaginations.</span></span>

<span data-ttu-id="bda78-155">在 Ajax 應用程式中，您不需要讓使用者體驗只是為了更新頁面而終止。</span><span class="sxs-lookup"><span data-stu-id="bda78-155">In an Ajax application, you don t need to bring the user experience to a halt just to update a page.</span></span> <span data-ttu-id="bda78-156">相反地，您可以在背景執行非同步要求來更新頁面。</span><span class="sxs-lookup"><span data-stu-id="bda78-156">Instead, you can perform an asynchronous request in the background to update the page.</span></span> <span data-ttu-id="bda78-157">您不會在部分頁面更新時強制使用者等待。</span><span class="sxs-lookup"><span data-stu-id="bda78-157">You don t force the user to wait while part of the page gets updated.</span></span>

<span data-ttu-id="bda78-158">藉由利用 Ajax，您也可以改善應用程式的效能。</span><span class="sxs-lookup"><span data-stu-id="bda78-158">By taking advantage of Ajax, you also can improve the performance of your application.</span></span> <span data-ttu-id="bda78-159">請考慮 Contact Manager 應用程式目前在沒有 Ajax 功能的情況下的運作方式。</span><span class="sxs-lookup"><span data-stu-id="bda78-159">Consider how the Contact Manager application works right now without Ajax functionality.</span></span> <span data-ttu-id="bda78-160">當您按一下連絡人群組時，必須重新顯示整個索引視圖。</span><span class="sxs-lookup"><span data-stu-id="bda78-160">When you click a contact group, the entire Index view must be redisplayed.</span></span> <span data-ttu-id="bda78-161">連絡人清單和連絡人群組清單必須從資料庫伺服器抓取。</span><span class="sxs-lookup"><span data-stu-id="bda78-161">The list of contacts and list of contact groups must be retrieved from the database server.</span></span> <span data-ttu-id="bda78-162">所有的資料都必須從 web 伺服器傳遞到網頁瀏覽器。</span><span class="sxs-lookup"><span data-stu-id="bda78-162">All of this data must be passed across the wire from web server to web browser.</span></span>

<span data-ttu-id="bda78-163">不過，在我們將 Ajax 功能新增至應用程式之後，我們可以避免在使用者按一下連絡人群組時重新出現整個頁面。</span><span class="sxs-lookup"><span data-stu-id="bda78-163">After we add Ajax functionality to our application, however, we can avoid redisplaying the entire page when a user clicks a contact group.</span></span> <span data-ttu-id="bda78-164">我們不再需要從資料庫抓取連絡人群組。</span><span class="sxs-lookup"><span data-stu-id="bda78-164">We no longer need to grab the contact groups from the database.</span></span> <span data-ttu-id="bda78-165">我們也不需要在網路上推送整個索引視圖。</span><span class="sxs-lookup"><span data-stu-id="bda78-165">We also don t need to push the entire Index view across the wire.</span></span> <span data-ttu-id="bda78-166">藉由利用 Ajax，我們減少了資料庫伺服器必須執行的工作量，並減少應用程式所需的網路流量。</span><span class="sxs-lookup"><span data-stu-id="bda78-166">By taking advantage of Ajax, we reduce the amount of work that our database server must perform and we reduce the amount of network traffic required by our application.</span></span>

## <a name="don-t-be-afraid-of-ajax"></a><span data-ttu-id="bda78-167">千萬別害怕 Ajax</span><span class="sxs-lookup"><span data-stu-id="bda78-167">Don t be Afraid of Ajax</span></span>

<span data-ttu-id="bda78-168">有些開發人員會避免使用 Ajax，因為他們會擔心舊版瀏覽器。</span><span class="sxs-lookup"><span data-stu-id="bda78-168">Some developers avoid using Ajax because they worry about downlevel browsers.</span></span> <span data-ttu-id="bda78-169">在不支援 JavaScript 的瀏覽器存取時，他們想要確保其 web 應用程式仍可運作。</span><span class="sxs-lookup"><span data-stu-id="bda78-169">They want to make sure that their web applications will still work when accessed by a browser that does not support JavaScript.</span></span> <span data-ttu-id="bda78-170">由於 Ajax 相依于 JavaScript，因此有些開發人員會避免使用 Ajax。</span><span class="sxs-lookup"><span data-stu-id="bda78-170">Because Ajax depends on JavaScript, some developers avoid using Ajax.</span></span>

<span data-ttu-id="bda78-171">不過，如果您想要瞭解如何執行 Ajax，則可以建立同時使用上層和下層瀏覽器的應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-171">However, if you are careful about how you implement Ajax then you can build applications that work with both uplevel and downlevel browsers.</span></span> <span data-ttu-id="bda78-172">我們的連絡人管理員應用程式將會使用支援 JavaScript 的瀏覽器，以及不支援的瀏覽器。</span><span class="sxs-lookup"><span data-stu-id="bda78-172">Our Contact Manager application will work with browsers that support JavaScript and browsers that do not.</span></span>

<span data-ttu-id="bda78-173">如果您使用 Contact Manager 應用程式搭配支援 JavaScript 的瀏覽器，則會有更好的使用者體驗。</span><span class="sxs-lookup"><span data-stu-id="bda78-173">If you use the Contact Manager application with a browser that supports JavaScript then you will have a better user experience.</span></span> <span data-ttu-id="bda78-174">例如，當您按一下連絡人群組時，只會更新顯示連絡人的頁面區域。</span><span class="sxs-lookup"><span data-stu-id="bda78-174">For example, when you click a contact group, only the region of the page that displays contacts will be updated.</span></span>

<span data-ttu-id="bda78-175">另一方面，如果您將 Contact Manager 應用程式與不支援 JavaScript 的瀏覽器搭配使用（或已停用 JavaScript），則您會有稍微不需要的使用者體驗。</span><span class="sxs-lookup"><span data-stu-id="bda78-175">If, on the other hand, you use the Contact Manager application with a browser that does not support JavaScript (or that has JavaScript disabled) then you will have a slightly less desirable user experience.</span></span> <span data-ttu-id="bda78-176">例如，當您按一下連絡人群組時，整個索引視圖必須回傳至瀏覽器，才能顯示相符的連絡人清單。</span><span class="sxs-lookup"><span data-stu-id="bda78-176">For example, when you click a contact group, the entire Index view must be posted back to the browser in order to display the matching list of contacts.</span></span>

## <a name="adding-the-required-javascript-files"></a><span data-ttu-id="bda78-177">新增必要的 JavaScript 檔案</span><span class="sxs-lookup"><span data-stu-id="bda78-177">Adding the Required JavaScript Files</span></span>

<span data-ttu-id="bda78-178">我們必須使用三個 JavaScript 檔案，將 Ajax 功能新增至我們的應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-178">We'll need to use three JavaScript files to add Ajax functionality to our application.</span></span> <span data-ttu-id="bda78-179">這三個檔案都包含在新 ASP.NET MVC 應用程式的 [腳本] 資料夾中。</span><span class="sxs-lookup"><span data-stu-id="bda78-179">All three of these files are included in the Scripts folder of a new ASP.NET MVC application.</span></span>

<span data-ttu-id="bda78-180">如果您打算在應用程式的多個頁面中使用 Ajax，則在您的應用程式 s 視圖主版頁面中包含必要的 JavaScript 檔案是合理的。</span><span class="sxs-lookup"><span data-stu-id="bda78-180">If you plan to use Ajax in multiple pages in your application then it makes sense to include the required JavaScript files in your application s view master page.</span></span> <span data-ttu-id="bda78-181">如此一來，JavaScript 檔案就會自動包含在應用程式的所有頁面中。</span><span class="sxs-lookup"><span data-stu-id="bda78-181">That way, the JavaScript files will be included in all of the pages in your application automatically.</span></span>

<span data-ttu-id="bda78-182">在您的視圖主版頁面的 &lt;標頭&gt; 標記中新增下列 JavaScript 包含：</span><span class="sxs-lookup"><span data-stu-id="bda78-182">Add the following JavaScript includes inside the &lt;head&gt; tag of your view master page:</span></span>

[!code-html[Main](iteration-7-add-ajax-functionality-vb/samples/sample1.html)]

## <a name="refactoring-the-index-view-to-use-ajax"></a><span data-ttu-id="bda78-183">重構索引視圖以使用 Ajax</span><span class="sxs-lookup"><span data-stu-id="bda78-183">Refactoring the Index View to use Ajax</span></span>

<span data-ttu-id="bda78-184">讓我們從修改索引視圖開始，讓按一下連絡人群組只會更新顯示連絡人的視圖區域。</span><span class="sxs-lookup"><span data-stu-id="bda78-184">Let s start by modifying our Index view so that clicking a contact group only updates the region of the view that displays contacts.</span></span> <span data-ttu-id="bda78-185">[圖 1] 中的紅色方塊包含我們想要更新的區域。</span><span class="sxs-lookup"><span data-stu-id="bda78-185">The red box in Figure 1 contains the region that we want to update.</span></span>

<span data-ttu-id="bda78-186">[![只更新連絡人](iteration-7-add-ajax-functionality-vb/_static/image1.jpg)](iteration-7-add-ajax-functionality-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="bda78-186">[![Updating only contacts](iteration-7-add-ajax-functionality-vb/_static/image1.jpg)](iteration-7-add-ajax-functionality-vb/_static/image1.png)</span></span>

<span data-ttu-id="bda78-187">**圖 01**：僅更新連絡人（[按一下以觀看完整大小的影像](iteration-7-add-ajax-functionality-vb/_static/image2.png)）</span><span class="sxs-lookup"><span data-stu-id="bda78-187">**Figure 01**: Updating only contacts([Click to view full-size image](iteration-7-add-ajax-functionality-vb/_static/image2.png))</span></span>

<span data-ttu-id="bda78-188">第一個步驟是將我們想要以非同步方式更新的視圖部分，分別放在不同的部分（view user control）。</span><span class="sxs-lookup"><span data-stu-id="bda78-188">The first step is to separate the part of the view that we want to update asynchronously into a separate partial (view user control).</span></span> <span data-ttu-id="bda78-189">顯示 [連絡人] 資料表的 [索引] 視圖區段已移至 [清單 1] 中的部分。</span><span class="sxs-lookup"><span data-stu-id="bda78-189">The section of the Index view that displays the table of contacts has been moved into the partial in Listing 1.</span></span>

<span data-ttu-id="bda78-190">**清單 1-Views\Contact\ContactList.ascx**</span><span class="sxs-lookup"><span data-stu-id="bda78-190">**Listing 1 - Views\Contact\ContactList.ascx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample2.aspx)]

<span data-ttu-id="bda78-191">請注意，[清單 1] 中的部分與 [索引] 視圖的模型不同。</span><span class="sxs-lookup"><span data-stu-id="bda78-191">Notice that the partial in Listing 1 has a different model than the Index view.</span></span> <span data-ttu-id="bda78-192">&lt;% @ Page%&gt; 指示詞中的*Inherits*屬性指定部分繼承自 ViewUserControl&lt;群組&gt; 類別。</span><span class="sxs-lookup"><span data-stu-id="bda78-192">The *Inherits* attribute in the &lt;%@ Page %&gt; directive specifies that the partial inherits from the ViewUserControl&lt;Group&gt; class.</span></span>

<span data-ttu-id="bda78-193">更新的索引視圖包含在 [清單 2] 中。</span><span class="sxs-lookup"><span data-stu-id="bda78-193">The updated Index view is contained in Listing 2.</span></span>

<span data-ttu-id="bda78-194">**清單 2-Views\Contact\Index.aspx**</span><span class="sxs-lookup"><span data-stu-id="bda78-194">**Listing 2 - Views\Contact\Index.aspx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample3.aspx)]

<span data-ttu-id="bda78-195">在 [清單 2] 中，您應該會注意到已更新的視圖有兩件事。</span><span class="sxs-lookup"><span data-stu-id="bda78-195">There are two things that you should notice about the updated view in Listing 2.</span></span> <span data-ttu-id="bda78-196">首先，請注意，移入部分的所有內容都會取代為 RenderPartial （）的呼叫。</span><span class="sxs-lookup"><span data-stu-id="bda78-196">First, notice that all of the content moved into the partial is replaced with a call to Html.RenderPartial().</span></span> <span data-ttu-id="bda78-197">第一次要求索引視圖以顯示初始連絡人集時，會呼叫 RenderPartial （）方法。</span><span class="sxs-lookup"><span data-stu-id="bda78-197">The Html.RenderPartial() method is called when the Index view is first requested in order to display the initial set of contacts.</span></span>

<span data-ttu-id="bda78-198">第二，請注意，用來顯示連絡人群組的 .Html （）已取代為 Html.actionlink （）。</span><span class="sxs-lookup"><span data-stu-id="bda78-198">Second, notice that the Html.ActionLink() used to display contact groups has been replaced with an Ajax.ActionLink().</span></span> <span data-ttu-id="bda78-199">使用下列參數呼叫了 Ajax （）：</span><span class="sxs-lookup"><span data-stu-id="bda78-199">The Ajax.ActionLink() is called with the following parameters:</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample4.aspx)]

<span data-ttu-id="bda78-200">第一個參數表示要顯示的連結文字，第二個參數代表路由值，而第三個參數代表 Ajax 選項。</span><span class="sxs-lookup"><span data-stu-id="bda78-200">The first parameter represents the text to display for the link, the second parameter represents the route values, and the third parameter represents the Ajax options.</span></span> <span data-ttu-id="bda78-201">在此情況下，我們會使用 UpdateTargetId Ajax 選項，指向在 Ajax 要求完成後要更新的 HTML &lt;div&gt; 標記。</span><span class="sxs-lookup"><span data-stu-id="bda78-201">In this case, we use the UpdateTargetId Ajax option to point to the HTML &lt;div&gt; tag that we want to update after the Ajax request completes.</span></span> <span data-ttu-id="bda78-202">我們想要使用新的連絡人清單來更新 &lt;div&gt; 標記。</span><span class="sxs-lookup"><span data-stu-id="bda78-202">We want to update the &lt;div&gt; tag with the new list of contacts.</span></span>

<span data-ttu-id="bda78-203">連絡人控制器的更新索引（）方法包含在 [清單 3] 中。</span><span class="sxs-lookup"><span data-stu-id="bda78-203">The updated Index() method of the Contact controller is contained in Listing 3.</span></span>

<span data-ttu-id="bda78-204">**清單 3-Controllers\ContactController.vb （Index 方法）**</span><span class="sxs-lookup"><span data-stu-id="bda78-204">**Listing 3 - Controllers\ContactController.vb (Index method)**</span></span>

[!code-vb[Main](iteration-7-add-ajax-functionality-vb/samples/sample5.vb)]

<span data-ttu-id="bda78-205">已更新的 Index （）動作會有條件地傳回兩個專案的其中一個。</span><span class="sxs-lookup"><span data-stu-id="bda78-205">The updated Index() action conditionally returns one of two things.</span></span> <span data-ttu-id="bda78-206">如果由 Ajax 要求叫用 Index （）動作，則控制器會傳回部分。</span><span class="sxs-lookup"><span data-stu-id="bda78-206">If the Index() action is invoked by an Ajax request then the controller returns a partial.</span></span> <span data-ttu-id="bda78-207">否則，Index （）動作會傳回整個視圖。</span><span class="sxs-lookup"><span data-stu-id="bda78-207">Otherwise, the Index() action returns an entire view.</span></span>

<span data-ttu-id="bda78-208">請注意，Index （）動作不需要傳回 Ajax 要求所叫用的資料量。</span><span class="sxs-lookup"><span data-stu-id="bda78-208">Notice that the Index() action does not need to return as much data when invoked by an Ajax request.</span></span> <span data-ttu-id="bda78-209">在一般要求的內容中，「索引」動作會傳回所有連絡人群組和所選連絡人群組的清單。</span><span class="sxs-lookup"><span data-stu-id="bda78-209">In the context of a normal request, the Index action returns a list of all of the contact groups and the selected contact group.</span></span> <span data-ttu-id="bda78-210">在 Ajax 要求的內容中，Index （）動作只會傳回選取的群組。</span><span class="sxs-lookup"><span data-stu-id="bda78-210">In the context of an Ajax request, the Index() action returns only the selected group.</span></span> <span data-ttu-id="bda78-211">Ajax 表示資料庫伺服器上的工作量較少。</span><span class="sxs-lookup"><span data-stu-id="bda78-211">Ajax means less work on your database server.</span></span>

<span data-ttu-id="bda78-212">在上層和舊版瀏覽器的情況下，我們修改過的索引視圖都適用。</span><span class="sxs-lookup"><span data-stu-id="bda78-212">Our modified Index view works in the case of both uplevel and downlevel browsers.</span></span> <span data-ttu-id="bda78-213">如果您按一下連絡人群組，而且瀏覽器支援 JavaScript，則只會更新包含連絡人清單的視圖區域。</span><span class="sxs-lookup"><span data-stu-id="bda78-213">If you click a contact group, and your browser supports JavaScript, then only the region of the view that contains the list of contacts is updated.</span></span> <span data-ttu-id="bda78-214">另一方面，如果您的瀏覽器不支援 JavaScript，則會更新整個視圖。</span><span class="sxs-lookup"><span data-stu-id="bda78-214">If, on the other hand, your browser does not support JavaScript, then the entire view is updated.</span></span>

<span data-ttu-id="bda78-215">我們更新的索引視圖有一個問題。</span><span class="sxs-lookup"><span data-stu-id="bda78-215">Our updated Index view has one problem.</span></span> <span data-ttu-id="bda78-216">當您按一下連絡人群組時，將不會反白顯示選取的群組。</span><span class="sxs-lookup"><span data-stu-id="bda78-216">When you click a contact group, the selected group is not highlighted.</span></span> <span data-ttu-id="bda78-217">因為群組清單會顯示在 Ajax 要求期間更新的區域之外，所以不會反白顯示正確的群組。</span><span class="sxs-lookup"><span data-stu-id="bda78-217">Because the list of groups is displayed outside of the region that is updated during an Ajax request, the right group does not get highlighted.</span></span> <span data-ttu-id="bda78-218">我們將在下一節修正此問題。</span><span class="sxs-lookup"><span data-stu-id="bda78-218">We'll fix this issue in the next section.</span></span>

## <a name="adding-jquery-animation-effects"></a><span data-ttu-id="bda78-219">新增 jQuery 動畫效果</span><span class="sxs-lookup"><span data-stu-id="bda78-219">Adding jQuery Animation Effects</span></span>

<span data-ttu-id="bda78-220">一般來說，當您按一下網頁中的連結時，可以使用瀏覽器的進度列來偵測瀏覽器是否正在主動提取更新的內容。</span><span class="sxs-lookup"><span data-stu-id="bda78-220">Normally, when you click a link in a web page, you can use the browser progress bar to detect whether or not the browser is actively fetching the updated content.</span></span> <span data-ttu-id="bda78-221">另一方面，執行 Ajax 要求時，瀏覽器的進度列不會顯示任何進度。</span><span class="sxs-lookup"><span data-stu-id="bda78-221">When performing an Ajax request, on the other hand, the browser progress bar does not show any progress.</span></span> <span data-ttu-id="bda78-222">這可讓使用者擔心。</span><span class="sxs-lookup"><span data-stu-id="bda78-222">This can make users nervous.</span></span> <span data-ttu-id="bda78-223">您如何知道瀏覽器是否已凍結？</span><span class="sxs-lookup"><span data-stu-id="bda78-223">How do you know whether the browser has frozen?</span></span>

<span data-ttu-id="bda78-224">有數種方式可讓您向使用者指出執行 Ajax 要求時所執行的工作。</span><span class="sxs-lookup"><span data-stu-id="bda78-224">There are several ways that you can indicate to a user that work is being performed while performing an Ajax request.</span></span> <span data-ttu-id="bda78-225">其中一個方法是顯示簡單的動畫。</span><span class="sxs-lookup"><span data-stu-id="bda78-225">One approach is to display a simple animation.</span></span> <span data-ttu-id="bda78-226">例如，您可以在 Ajax 要求開始時淡出區域，並在要求完成時淡入區域。</span><span class="sxs-lookup"><span data-stu-id="bda78-226">For example, you can fade out a region when an Ajax request begins and fade in the region when the request completes.</span></span>

<span data-ttu-id="bda78-227">我們將使用 Microsoft ASP.NET MVC 架構隨附的 jQuery 程式庫來建立動畫效果。</span><span class="sxs-lookup"><span data-stu-id="bda78-227">We'll use the jQuery library which is included with the Microsoft ASP.NET MVC framework, to create the animation effects.</span></span> <span data-ttu-id="bda78-228">更新的索引視圖包含在 [清單 4] 中。</span><span class="sxs-lookup"><span data-stu-id="bda78-228">The updated Index view is contained in Listing 4.</span></span>

<span data-ttu-id="bda78-229">**清單 4-Views\Contact\Index.aspx**</span><span class="sxs-lookup"><span data-stu-id="bda78-229">**Listing 4 - Views\Contact\Index.aspx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample6.aspx)]

<span data-ttu-id="bda78-230">請注意，更新的索引視圖包含三個新的 JavaScript 函數。</span><span class="sxs-lookup"><span data-stu-id="bda78-230">Notice that the updated Index view contains three new JavaScript functions.</span></span> <span data-ttu-id="bda78-231">當您按一下新的連絡人群組時，前兩個函式會使用 jQuery 淡出並淡入連絡人清單。</span><span class="sxs-lookup"><span data-stu-id="bda78-231">The first two functions use jQuery to fade out and fade in the list of contacts when you click a new contact group.</span></span> <span data-ttu-id="bda78-232">當 Ajax 要求導致錯誤（例如網路超時）時，第三個函式會顯示錯誤訊息。</span><span class="sxs-lookup"><span data-stu-id="bda78-232">The third function displays an error message when an Ajax request results in an error (for example, network timeout).</span></span>

<span data-ttu-id="bda78-233">第一個函式也會負責反白顯示選取的群組。</span><span class="sxs-lookup"><span data-stu-id="bda78-233">The first function also takes care of highlighting the selected group.</span></span> <span data-ttu-id="bda78-234">Class = selected 屬性會加入至所按元素的父元素（LI 元素）。</span><span class="sxs-lookup"><span data-stu-id="bda78-234">A class= selected attribute is added to the parent element (the LI element) of the element clicked.</span></span> <span data-ttu-id="bda78-235">同樣地，jQuery 可讓您輕鬆選取正確的專案，並加入 CSS 類別。</span><span class="sxs-lookup"><span data-stu-id="bda78-235">Again, jQuery makes it easy to select the right element and add the CSS class.</span></span>

<span data-ttu-id="bda78-236">這些腳本會透過 AjaxOptions 參數的協助，系結至群組連結。</span><span class="sxs-lookup"><span data-stu-id="bda78-236">These scripts are tied to the group links with the help of the Ajax.ActionLink() AjaxOptions parameter.</span></span> <span data-ttu-id="bda78-237">更新的 Ajax （）方法呼叫看起來像這樣：</span><span class="sxs-lookup"><span data-stu-id="bda78-237">The updated Ajax.ActionLink() method call looks like this:</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample7.aspx)]

## <a name="adding-browser-history-support"></a><span data-ttu-id="bda78-238">新增瀏覽器歷程記錄支援</span><span class="sxs-lookup"><span data-stu-id="bda78-238">Adding Browser History Support</span></span>

<span data-ttu-id="bda78-239">一般來說，當您按一下連結來更新頁面時，您的瀏覽器歷程記錄就會更新。</span><span class="sxs-lookup"><span data-stu-id="bda78-239">Normally, when you click a link to update a page, your browser history is updated.</span></span> <span data-ttu-id="bda78-240">如此一來，您可以按一下瀏覽器的 [上一頁] 按鈕，將時間移回到頁面的先前狀態。</span><span class="sxs-lookup"><span data-stu-id="bda78-240">That way, you can click the browser Back button to move back in time to the previous state of the page.</span></span> <span data-ttu-id="bda78-241">例如，如果您按一下 [friend 連絡人] 群組，然後按一下 [商務連絡人] 群組，則可以按一下 [瀏覽器上一頁] 按鈕，在選取 [朋友連絡人] 群組時，流覽回頁面的狀態。</span><span class="sxs-lookup"><span data-stu-id="bda78-241">For example, if you click the Friends contact group and then click the Business contact group, you can click the browser Back button to navigate back to the state of the page when the Friends contact group was selected.</span></span>

<span data-ttu-id="bda78-242">可惜的是，執行 Ajax 要求並不會自動更新瀏覽器記錄。</span><span class="sxs-lookup"><span data-stu-id="bda78-242">Unfortunately, performing an Ajax request does not update browser history automatically.</span></span> <span data-ttu-id="bda78-243">如果您按一下連絡人群組，並使用 Ajax 要求來抓取相符連絡人清單，則不會更新瀏覽器記錄。</span><span class="sxs-lookup"><span data-stu-id="bda78-243">If you click a contact group, and the list of matching contacts is retrieved with an Ajax request, then the browser history is not updated.</span></span> <span data-ttu-id="bda78-244">選取新的連絡人群組之後，您就無法使用瀏覽器的 [上一頁] 按鈕來流覽回連絡人群組。</span><span class="sxs-lookup"><span data-stu-id="bda78-244">You cannot use the browser Back button to navigate back to a contact group after selecting a new contact group.</span></span>

<span data-ttu-id="bda78-245">如果您想要讓使用者在執行 Ajax 要求之後能夠使用瀏覽器的 [上一頁] 按鈕，則您需要執行更多工作。</span><span class="sxs-lookup"><span data-stu-id="bda78-245">If you want users to be able to use the browser Back button after performing Ajax requests then you need to perform a little more work.</span></span> <span data-ttu-id="bda78-246">您必須利用 ASP.NET AJAX 架構內建的瀏覽器歷程記錄管理功能。</span><span class="sxs-lookup"><span data-stu-id="bda78-246">You need to take advantage of the browser history management functionality built in the ASP.NET AJAX Framework.</span></span>

<span data-ttu-id="bda78-247">ASP.NET AJAX browser 歷程記錄，您需要做三件事：</span><span class="sxs-lookup"><span data-stu-id="bda78-247">ASP.NET AJAX browser history, you need to do three things:</span></span>

1. <span data-ttu-id="bda78-248">將 enableBrowserHistory 屬性設定為 true，以啟用瀏覽器歷程記錄。</span><span class="sxs-lookup"><span data-stu-id="bda78-248">Enable Browser History by setting the enableBrowserHistory property to true.</span></span>
2. <span data-ttu-id="bda78-249">當視圖的狀態藉由呼叫 addHistoryPoint （）方法來變更時，儲存歷程記錄點。</span><span class="sxs-lookup"><span data-stu-id="bda78-249">Save history points when the state of a view changes by calling the addHistoryPoint() method.</span></span>
3. <span data-ttu-id="bda78-250">當引發導覽事件時，重建視圖的狀態。</span><span class="sxs-lookup"><span data-stu-id="bda78-250">Reconstruct the state of the view when the navigate event is raised.</span></span>

<span data-ttu-id="bda78-251">更新的索引視圖包含在 [清單 5] 中。</span><span class="sxs-lookup"><span data-stu-id="bda78-251">The updated Index view is contained in Listing 5.</span></span>

<span data-ttu-id="bda78-252">**清單 5-Views\Contact\Index.aspx**</span><span class="sxs-lookup"><span data-stu-id="bda78-252">**Listing 5 - Views\Contact\Index.aspx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample8.aspx)]

<span data-ttu-id="bda78-253">在 [清單 5] 中，已在 pageInit （）函數中啟用瀏覽器歷程記錄。</span><span class="sxs-lookup"><span data-stu-id="bda78-253">In Listing 5, Browser History is enabled in the pageInit() function.</span></span> <span data-ttu-id="bda78-254">PageInit （）函數也用來設定導覽事件的事件處理常式。</span><span class="sxs-lookup"><span data-stu-id="bda78-254">The pageInit() function is also used to set up the event handler for the navigate event.</span></span> <span data-ttu-id="bda78-255">每當瀏覽器的 [轉寄] 或 [上一頁] 按鈕導致頁面的狀態變更時，就會引發 [導覽] 事件。</span><span class="sxs-lookup"><span data-stu-id="bda78-255">The navigate event is raised whenever the browser Forward or Back button causes the state of the page to change.</span></span>

<span data-ttu-id="bda78-256">當您按一下連絡人群組時，會呼叫 beginContactList （）方法。</span><span class="sxs-lookup"><span data-stu-id="bda78-256">The beginContactList() method is called when you click a contact group.</span></span> <span data-ttu-id="bda78-257">這個方法會藉由呼叫 addHistoryPoint （）方法來建立新的記錄點。</span><span class="sxs-lookup"><span data-stu-id="bda78-257">This method creates a new history point by calling the addHistoryPoint() method.</span></span> <span data-ttu-id="bda78-258">已按下之連絡人群組的識別碼會新增至 [歷程記錄]。</span><span class="sxs-lookup"><span data-stu-id="bda78-258">The id of the contact group clicked is added to history.</span></span>

<span data-ttu-id="bda78-259">系統會從 contact 群組連結上的 expando 屬性來抓取群組識別碼。</span><span class="sxs-lookup"><span data-stu-id="bda78-259">The group id is retrieved from an expando attribute on the contact group link.</span></span> <span data-ttu-id="bda78-260">連結會以下列對 Ajax 的呼叫來呈現（）。</span><span class="sxs-lookup"><span data-stu-id="bda78-260">The link is rendered with the following call to Ajax.ActionLink().</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample9.aspx)]

<span data-ttu-id="bda78-261">傳遞至 Ajax 的最後一個參數（）會將名為 groupid 的 expando 屬性加入至連結（XHTML 相容性為小寫）。</span><span class="sxs-lookup"><span data-stu-id="bda78-261">The last parameter passed to the Ajax.ActionLink() adds an expando attribute named groupid to the link (lowercase for XHTML compatibility).</span></span>

<span data-ttu-id="bda78-262">當使用者點擊瀏覽器的 [上一頁] 或 [下一頁] 按鈕時，就會引發導覽事件並呼叫導覽（）方法。</span><span class="sxs-lookup"><span data-stu-id="bda78-262">When a user hits the browser Back or Forward button, the navigate event is raised and the navigate() method is called.</span></span> <span data-ttu-id="bda78-263">這個方法會更新頁面中顯示的連絡人，以符合與傳遞至導覽方法之瀏覽器歷程記錄點對應的頁面狀態。</span><span class="sxs-lookup"><span data-stu-id="bda78-263">This method updates the contacts displayed in the page to match the state of the page that corresponds to the browser history point passed to the navigate method.</span></span>

## <a name="performing-ajax-deletes"></a><span data-ttu-id="bda78-264">執行 Ajax 刪除</span><span class="sxs-lookup"><span data-stu-id="bda78-264">Performing Ajax Deletes</span></span>

<span data-ttu-id="bda78-265">目前若要刪除連絡人，您必須按一下 [刪除] 連結，然後按一下 [刪除確認] 頁面中顯示的 [刪除] 按鈕（請參閱 [圖 2]）。</span><span class="sxs-lookup"><span data-stu-id="bda78-265">Currently, in order to delete a contact, you need to click on the Delete link and then click the Delete button displayed in the delete confirmation page (see Figure 2).</span></span> <span data-ttu-id="bda78-266">這似乎很多頁面要求，可以執行像是刪除資料庫記錄的簡單動作。</span><span class="sxs-lookup"><span data-stu-id="bda78-266">This seems like a lot of page requests to do something simple like deleting a database record.</span></span>

<span data-ttu-id="bda78-267">[![[刪除確認] 頁面](iteration-7-add-ajax-functionality-vb/_static/image2.jpg)](iteration-7-add-ajax-functionality-vb/_static/image3.png)</span><span class="sxs-lookup"><span data-stu-id="bda78-267">[![The delete confirmation page](iteration-7-add-ajax-functionality-vb/_static/image2.jpg)](iteration-7-add-ajax-functionality-vb/_static/image3.png)</span></span>

<span data-ttu-id="bda78-268">**圖 02**：刪除確認頁面（[按一下以查看完整大小的影像](iteration-7-add-ajax-functionality-vb/_static/image4.png)）</span><span class="sxs-lookup"><span data-stu-id="bda78-268">**Figure 02**: The delete confirmation page([Click to view full-size image](iteration-7-add-ajax-functionality-vb/_static/image4.png))</span></span>

<span data-ttu-id="bda78-269">略過刪除確認頁面，並直接從索引視圖刪除連絡人是很吸引人的。</span><span class="sxs-lookup"><span data-stu-id="bda78-269">It is tempting to skip the delete confirmation page and delete a contact directly from the Index view.</span></span> <span data-ttu-id="bda78-270">您應該避免這個誘惑，因為採取此方法會將您的應用程式開啟至安全性漏洞。</span><span class="sxs-lookup"><span data-stu-id="bda78-270">You should avoid this temptation because taking this approach opens your application to security holes.</span></span> <span data-ttu-id="bda78-271">一般來說，您不會想要在叫用修改 web 應用程式狀態的動作時，執行 HTTP GET 作業。</span><span class="sxs-lookup"><span data-stu-id="bda78-271">In general, you don t want to perform an HTTP GET operation when invoking an action that modifies the state of your web application.</span></span> <span data-ttu-id="bda78-272">執行刪除時，您會想要執行 HTTP POST，或更好的 HTTP 刪除作業。</span><span class="sxs-lookup"><span data-stu-id="bda78-272">When performing a delete, you want to perform an HTTP POST, or better yet, an HTTP DELETE operation.</span></span>

<span data-ttu-id="bda78-273">[刪除] 連結包含在 ContactList 部分中。</span><span class="sxs-lookup"><span data-stu-id="bda78-273">The Delete link is contained in the ContactList partial.</span></span> <span data-ttu-id="bda78-274">[清單 6] 中包含 ContactList 部分的更新版本。</span><span class="sxs-lookup"><span data-stu-id="bda78-274">An updated version of the ContactList partial is contained in Listing 6.</span></span>

<span data-ttu-id="bda78-275">**清單 6-Views\Contact\ContactList.ascx**</span><span class="sxs-lookup"><span data-stu-id="bda78-275">**Listing 6 - Views\Contact\ContactList.ascx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample10.aspx)]

<span data-ttu-id="bda78-276">[刪除] 連結會使用下列 ImageActionLink （）方法的呼叫來呈現：</span><span class="sxs-lookup"><span data-stu-id="bda78-276">The Delete link is rendered with the following call to the Ajax.ImageActionLink() method:</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample11.aspx)]

> [!NOTE] 
> 
> <span data-ttu-id="bda78-277">ImageActionLink （）不是 ASP.NET MVC 架構的標準部分。</span><span class="sxs-lookup"><span data-stu-id="bda78-277">The Ajax.ImageActionLink() is not a standard part of the ASP.NET MVC framework.</span></span> <span data-ttu-id="bda78-278">ImageActionLink （）是包含在 Contact Manager 專案中的自訂 helper 方法。</span><span class="sxs-lookup"><span data-stu-id="bda78-278">The Ajax.ImageActionLink() is a custom helper methods included in the Contact Manager project.</span></span>

<span data-ttu-id="bda78-279">AjaxOptions 參數有兩個屬性。</span><span class="sxs-lookup"><span data-stu-id="bda78-279">The AjaxOptions parameter has two properties.</span></span> <span data-ttu-id="bda78-280">首先，Confirm 屬性是用來顯示快捷方式 JavaScript 確認對話方塊。</span><span class="sxs-lookup"><span data-stu-id="bda78-280">First, the Confirm property is used to display a popup JavaScript confirmation dialog.</span></span> <span data-ttu-id="bda78-281">第二，HttpMethod 屬性是用來執行 HTTP 刪除作業。</span><span class="sxs-lookup"><span data-stu-id="bda78-281">Second, the HttpMethod property is used to perform an HTTP DELETE operation.</span></span>

<span data-ttu-id="bda78-282">[清單 7] 包含新的 AjaxDelete （）動作，已新增至連絡人控制器。</span><span class="sxs-lookup"><span data-stu-id="bda78-282">Listing 7 contains a new AjaxDelete() action that has been added to the Contact controller.</span></span>

<span data-ttu-id="bda78-283">**清單 7-Controllers\ContactController.vb （AjaxDelete）**</span><span class="sxs-lookup"><span data-stu-id="bda78-283">**Listing 7 - Controllers\ContactController.vb (AjaxDelete)**</span></span>   

[!code-vb[Main](iteration-7-add-ajax-functionality-vb/samples/sample12.vb)]

<span data-ttu-id="bda78-284">AjaxDelete （）動作會以 AcceptVerbs 屬性裝飾。</span><span class="sxs-lookup"><span data-stu-id="bda78-284">The AjaxDelete() action is decorated with an AcceptVerbs attribute.</span></span> <span data-ttu-id="bda78-285">除了 HTTP 刪除作業以外的任何 HTTP 作業以外，此屬性可防止叫用動作。</span><span class="sxs-lookup"><span data-stu-id="bda78-285">This attribute prevents the action from being invoked except by any HTTP operation other than an HTTP DELETE operation.</span></span> <span data-ttu-id="bda78-286">特別是，您無法使用 HTTP GET 叫用此動作。</span><span class="sxs-lookup"><span data-stu-id="bda78-286">In particular, you cannot invoke this action with an HTTP GET.</span></span>

<span data-ttu-id="bda78-287">刪除資料庫記錄之後，您必須顯示未包含已刪除記錄之連絡人的更新清單。</span><span class="sxs-lookup"><span data-stu-id="bda78-287">After you delete database record, you need to display the updated list of contacts that does not contain the deleted record.</span></span> <span data-ttu-id="bda78-288">AjaxDelete （）方法會傳回 ContactList 部分和更新的連絡人清單。</span><span class="sxs-lookup"><span data-stu-id="bda78-288">The AjaxDelete() method returns the ContactList partial and the updated list of contacts.</span></span>

## <a name="summary"></a><span data-ttu-id="bda78-289">總結</span><span class="sxs-lookup"><span data-stu-id="bda78-289">Summary</span></span>

<span data-ttu-id="bda78-290">在此反復專案中，我們已將 Ajax 功能新增至我們的 Contact Manager 應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-290">In this iteration, we added Ajax functionality to our Contact Manager application.</span></span> <span data-ttu-id="bda78-291">我們使用 Ajax 來改善應用程式的回應性和效能。</span><span class="sxs-lookup"><span data-stu-id="bda78-291">We used Ajax to improve the responsiveness and performance of our application.</span></span>

<span data-ttu-id="bda78-292">首先，我們重構了索引視圖，因此按一下連絡人群組並不會更新整個視圖。</span><span class="sxs-lookup"><span data-stu-id="bda78-292">First, we refactored the Index view so that clicking a contact group does not update the entire view.</span></span> <span data-ttu-id="bda78-293">相反地，按一下連絡人群組只會更新連絡人清單。</span><span class="sxs-lookup"><span data-stu-id="bda78-293">Instead, clicking a contact group only updates the list of contacts.</span></span>

<span data-ttu-id="bda78-294">接下來，我們使用 jQuery 動畫效果淡出並淡入連絡人清單中。</span><span class="sxs-lookup"><span data-stu-id="bda78-294">Next, we used jQuery animation effects to fade out and fade in the list of contacts.</span></span> <span data-ttu-id="bda78-295">將動畫新增至 Ajax 應用程式，可以用來提供應用程式的使用者，使其具有對等的瀏覽器進度列。</span><span class="sxs-lookup"><span data-stu-id="bda78-295">Adding animation to an Ajax application can be used to provide users of the application with the equivalent of a browser progress bar.</span></span>

<span data-ttu-id="bda78-296">我們也將瀏覽器歷程記錄支援新增至我們的 Ajax 應用程式。</span><span class="sxs-lookup"><span data-stu-id="bda78-296">We also added browser history support to our Ajax application.</span></span> <span data-ttu-id="bda78-297">我們已讓使用者按一下瀏覽器的 [上一頁] 和 [下一頁] 按鈕，以變更索引視圖的狀態。</span><span class="sxs-lookup"><span data-stu-id="bda78-297">We enabled users to click the browser Back and Forward buttons to change the state of the Index view.</span></span>

<span data-ttu-id="bda78-298">最後，我們建立了支援 HTTP 刪除作業的刪除連結。</span><span class="sxs-lookup"><span data-stu-id="bda78-298">Finally, we created a delete link that supports HTTP DELETE operations.</span></span> <span data-ttu-id="bda78-299">藉由執行 Ajax delete，我們可以讓使用者刪除資料庫記錄，而不需要使用者要求額外的刪除確認頁面。</span><span class="sxs-lookup"><span data-stu-id="bda78-299">By performing Ajax deletes, we enable users to delete database records without requiring the user to request an additional delete confirmation page.</span></span>

> [!div class="step-by-step"]
> [<span data-ttu-id="bda78-300">上一篇</span><span class="sxs-lookup"><span data-stu-id="bda78-300">Previous</span></span>](iteration-6-use-test-driven-development-vb.md)
